# EnvShield Backlog

This is the durable, itemized engineering record: confirmed bugs, security
issues, limitations, feature requests, architectural questions, performance
unknowns, and strategic directions discovered through EnvShield development,
Zeus integration, external user feedback, competitive research, and audits.

**This is not the roadmap.** [ROADMAP.md](ROADMAP.md) is curated public
direction (what ships, what's next, what's explicitly not being built).
This file is the operational ledger behind it — every individual finding,
its evidence, and its status, so nothing gets silently lost or silently
promoted. ROADMAP.md should eventually *reference* major themes from here;
this pass does not rewrite ROADMAP.md.

**Rule zero:** never convert one evidence status into another without
re-stating the evidence. A `NEEDS_EVIDENCE` item becomes `CONFIRMED` only
by recording what was reproduced and how. A `CONFIRMED` item is never
deleted when fixed — it's marked `Decision: Fixed in vX.Y.Z` and kept, so a
regression can never quietly reintroduce something already found once.

## Evidence status legend

| Status | Meaning |
|---|---|
| `CONFIRMED` | Directly reproduced or verified against current source, live or via direct code reading with citation. |
| `NEEDS_EVIDENCE` | Plausible, sourced from a document or inference, not independently verified this pass. |
| `STALE` | Was true in an older version/state; no longer true. |
| `REFUTED` | Tested and found not to be true. Kept, not deleted, so it isn't re-raised. |
| `STRATEGIC` | Product/architecture direction, not a current defect. |
| `USER_FEEDBACK` | External user report/question that may indicate a product requirement. |
| `RESEARCH` | Competitive/market observation. |

## ID scheme

`BL-NNN` is the stable, primary ID going forward. Where a finding was
already named elsewhere (the original Zeus audit's `G-N`, the 2026-08-21
release-readiness audit's `RA-PN-N`, this engagement's own `EB-N`/`G3-N`/
`AD-N`/`FUT-N`), that name is kept alongside as an alias — never orphan a
finding from where it was first discovered.

---

## Part 0 — Release Blockers

**Do not claim EnvShield is release-ready while any of these remain open.**
All four are live-reproduced this engagement, not inferred. **Status as of
2026-08-26: all four (`BL-001`, `BL-002`, `BL-003`, `BL-004`) fixed in code
and committed locally (commits `63d6581`, `77be1e5`, `f478f9f`, `134ef7e`),
not yet released — see each item's own entry.** Independently re-verified
against the current working tree this same day (post-commit verification
pass): each fix's underlying failure class, not just the original
reproduction, was re-tested live and confirmed still correct; see each
item's own `Decision` line. None of the four have been released yet, and
"all four blockers fixed" does not by itself mean a release should be
proposed — see this file's own "Versioning and Release Cadence" reasoning
in `CLAUDE.md`/`AGENTS.md`.

### BL-001 (aka RA-P0-4) — Secret + `defaultValue` propagates into committed/generated output
Type: `SECURITY` · Evidence: `CONFIRMED` · Priority: **P0**
Source(s): 2026-08-21 release-readiness audit; independently live-reproduced this engagement's own verification pass.
Component: `schema_types.py` (shared `secret_default_conflict` predicate), `config/manager.py` (schema-load-time rejection — the fix's primary gate), `schema_manager.py` (`schema sync`'s `.env.example` body, `check`'s `_source_label`), `generator.py` (Python/TypeScript codegen), `explain.py` (`_describe_field`)

- **Problem:** A schema field with `secret = true` and a real `defaultValue` was accepted with no rejection anywhere.
- **Current behavior (fixed 2026-08-26):** Previously the value was written verbatim into `.env.example` (`schema sync`), into generated Python (`pydantic-settings` field default), and into generated TypeScript (`z.string().default(...)`, embedded in the literal before the `Secret<T>` wrapper is even applied at export). While tracing every consumer of a field's `defaultValue` for this fix, two further, previously-undiscovered manifestations of the same root cause were found and fixed in the same pass: `envshield explain`'s field description (`explain.py::_describe_field`, both Rich and `--json`) and `check`'s "Missing in Local"/"Blank in Local" Rich table (`schema_manager.py::_source_label`) both also echoed a secret field's real default with no check against the `secret` flag.
- **Root cause:** No code path anywhere consulted the `secret` flag before echoing `defaultValue`. The five manifestations above shared one implementation boundary that made a single fix possible: every schema-consuming command (`check`/`doctor`/`setup`/`schema sync`/`generate`/`explain`/`scan`) already loads its schema through exactly two entry points, `config_manager.load_schema`/`load_bare_schema` → `_load_schema_file`.
- **Fix:** Two-layer defense-in-depth, no new abstraction — both layers reuse a single new pure predicate, `schema_types.secret_default_conflict(field_schema)` (mirrors an invariant `importer.py`'s schema *generation* already enforced unconditionally: a variable classified secret never got a `defaultValue` in the first place).
  1. **Primary gate:** `config_manager.load_schema`/`load_bare_schema` now call `_reject_secret_defaults` immediately after resolving the schema (post-`extends`-merge), raising a new `SecretDefaultConflictError` naming the offending field(s) — never the value itself — if any field is `secret` with a non-empty `defaultValue`. This closes all five manifestations (and any future one) for every real command in one place, the same way an unsafe schema key name is already rejected outright rather than left for each generator to notice.
  2. **Defense in depth:** each of the five original render sites also independently treats a secret field's conflicting default as absent (masks/omits it) via the same predicate, so a schema dict that reaches `generate_config`/`sync_schema`/`_source_label`/`_describe_field` directly (bypassing the loader — which is exactly how the existing test suite's own `mocker.patch("envshield.config.manager.load_schema", return_value=...)` pattern works) still can't leak.
  - **Explicitly out of scope, by design:** the *local* Python secrets file (`_sync_python_local_file` / `setup_manager._fill_schema_defaults`) — that file is the intended destination for a real secret value (created via `file_updater.open_new_secret_file`, 0600 permissions, not committed), so a secret's `defaultValue` seeding it is correct behavior, not a leak. `schema_snapshot.py`'s explicit-revision path (`schema diff` against a historical Git revision, via `git show`) is also untouched — it must still be able to represent a past, possibly-broken schema state for comparison; only the live-working-tree path (`revision=None`, which already delegates to `config_manager.load_schema`) is gated.
- **Evidence/reproduction:** Original bug live-reproduced with a synthetic marker (`SYNTHETIC_NOT_A_SECRET_123` / a fake value) across all three originally-reported outputs in an isolated `/tmp` project. Fix independently live-reproduced 2026-08-26 against the real `envshield` console script (not just `pytest`) in a fresh isolated `/tmp` project with a `sk_live_SYNTHETIC_NOT_A_REAL_SECRET` marker: `check`/`schema sync`/`generate --lang python`/`generate --lang typescript`/`explain` all now refuse to load with a clear, non-leaking `SecretDefaultConflictError` and exit 1; after removing the conflicting default, all five commands succeed, `grep -r` for the marker across every generated artifact (`.env.example`, `config.py`, `config.ts`) found nothing, the field renders correctly as required (`...`/`z.string().min(1)`) and still typed `SecretStr`/`Secret<T>`, and a sibling non-secret default (`LOG_LEVEL=info`) was confirmed completely unaffected in all outputs.
- **Impact:** `.env.example` and generated config modules are exactly the files EnvShield's own workflow instructs a user to commit; `explain --json` and `check`'s terminal output are exactly the surfaces the charter's §4 invariants name directly ("never expose secret values in normal output," "...in JSON"). Directly violated the charter's own §4 invariant ("never store secret values in the configuration contract").
- **Dependencies:** None.
- **Related:** BL-070 (the DEV.to comment that first raised the conceptual question this bug concretely answers).
- **Tests:** Regression coverage added for the shared predicate (`test_schema_types.py::TestSecretDefaultConflict`, 6 cases including non-string TOML defaults and the empty-string non-conflict edge case), the primary load-time gate (`test_config_manager.py::TestLoadSchemaRejectsSecretDefaults`, 6 cases including an `extends`-inherited conflict and two non-secret/no-conflict sanity checks), and each of the five defense-in-depth render sites (`test_generator.py` ×2 — Python and TypeScript, `test_schema_manager.py` ×2 — `.env.example` sync and `_source_label`, `test_explain.py` ×1). Two pre-existing tests were found to lock in the old (buggy) behavior as their expected result and were corrected rather than left failing: `test_generator.py::test_generate_config_secret_with_default_stays_secret_str` previously asserted the literal secret default appeared in generated Python; `test_setup_manager.py::test_setup_requiredif_explanation_never_leaks_a_secret_triggers_value` and `test_cli.py::test_init_force_preserves_a_hand_corrected_secret_classification` both constructed now-invalid fixture schemas (a secret field with an unrelated defaultValue in the first; an unscoped string-replace that marked three importer-generated fields secret while leaving their inferred defaults in place in the second) — both fixture schemas were narrowed to no longer create the conflict, without weakening either test's actual assertion. Full suite: 979/979 passing. `ruff check .`: clean.
- **Decision:** **Fixed 2026-08-26.** Kept here, not removed, as the record of what was found and fixed — per the charter's own rule that a `CONFIRMED` finding is marked `Decision: Fixed in vX.Y.Z` and kept, never deleted, so a regression can't quietly reintroduce this.
- **Target:** Done. Not yet released — see CLAUDE.md/AGENTS.md §19's release-status note (`v4.6.0` tagged but not live on PyPI; this fix is a further local commit on top of it, same as BL-001–BL-004's siblings once each is fixed).
- **Notes:** This directly contradicted CLAUDE.md's own summary of the 2026-08-21 audit ("no secret leakage... findings"). **Resolved 2026-08-26 (documentation):** CLAUDE.md §19 (and its AGENTS.md counterpart) corrected to state this and the other three Part 0 blockers explicitly, rather than claiming none exist. **Resolved 2026-08-26 (code):** the bug itself, and the two additional manifestations discovered while fixing it, are fixed per above — this is the first of the four Part 0 blockers to move from "documented as open" to actually closed.

### BL-002 (aka RA-P1-4) — Malformed Python config breaks the `--json` contract and can produce a false clean
Type: `RELIABILITY` · Evidence: `CONFIRMED` · Priority: **P0**
Source(s): 2026-08-21 audit; independently live-reproduced; re-investigated and re-reproduced 2026-08-26 (implementation not yet done — see Decision).
Component: `parsers/_python.py` (`PythonParser.get_vars`), surfaced through `check`/`check --json` and `doctor`/`doctor --json`; also reachable, with different consequences, through `setup`, `schema sync`, `explain`, `service_discovery`, and `import` — see "Affected callers" below.

- **Problem:** `PythonParser.get_vars()` catches `SyntaxError`/`TypeError` internally, `print()`s a warning to stdout, and returns an empty result rather than raising.
- **Current behavior:** `check --json` prints the warning line *before* the JSON document (stdout is not valid single-document JSON); with an empty schema (or a schema where every field has a default), the command exits 0 with `"success": true`. The same warning-before-JSON break also reproduces in `doctor --json` — a second command, not named in the original report.
- **Expected behavior:** A parse failure becomes a structured command error; stdout remains exactly one JSON document; exit code is non-zero regardless of schema shape.
- **Evidence/reproduction (2026-08-21, and re-confirmed 2026-08-26 against current `main`, unchanged since first reported):** Live-reproduced with an unterminated-string `.py` fixture via the real `envshield` console script (not just source reading): with an empty schema, `check --json` returns `"success": true, "clean": true` (exit 0) despite the local file being unparseable — the false-clean, reproduced exactly. With any schema that has at least one field without a matching value (required, or defaulted — `should_be_present` still requires a defaulted field's presence), the field correctly shows as `missing` and `success`/`clean` are `false` (exit 1) — no false-clean in that case. In every case, stdout is corrupted: the `Warning: Could not parse...` line prints before the JSON document (confirmed on real stdout, not stderr, via redirection), so `json.loads(stdout)` always raises `JSONDecodeError` regardless of schema shape. `doctor --json` reproduces the identical stdout corruption.
- **Root cause:** `PythonParser.get_vars` is the only parser in `envshield/parsers/` that converts a parse failure into a silent empty result instead of raising. `DockerComposeParser.get_vars` (`_docker_compose.py`) already does the correct thing for the equivalent case — catches `yaml.YAMLError` and raises `EnvShieldException` with a descriptive message — and every caller of `get_vars` across the codebase is already written expecting that contract (see below). This is a single, local inconsistency, not a design gap needing new architecture: the fix is to make `PythonParser.get_vars` match its sibling's existing, already-correct pattern.
- **Affected callers, traced exhaustively (`grep -rn "\.get_vars("`), and how each currently behaves once `PythonParser` raises `EnvShieldException` instead of swallowing:**
  - **Already correct, no change needed** — each already has an `except (..., EnvShieldException, ...)` clause at the point that matters, or the call sits inside a function whose CLI command already wraps the whole body in `except EnvShieldException`: `schema_manager.check_schema`/`check_result` (`schema_manager.py:247,340` — both already have `except (ValueError, EnvShieldException) as e:`), `doctor._check_local_env_sync`/`_check_deployment_manifest` (`doctor.py:100,139` — already handle `EnvShieldException`, and `HealthCheck.run()` itself also catches `EnvShieldException` around every check function as a second layer), `setup_manager._read_seed_values`/`_write_local_env_file` (`setup_manager.py:127,418` — no local catch, but every CLI entry point that reaches `run_setup` — `setup`, `init` — already wraps the call in `except EnvShieldException`), `schema_manager._sync_python_local_file` (`schema_manager.py:558` — same: `sync_schema`'s CLI callers in `schema sync`, `init`, and `import` already wrap it).
  - **Needs a companion one-line fix, or the exception regresses an existing "skip and continue" behavior into an unwanted hard failure:** three call sites currently catch a narrower exception tuple that doesn't include `EnvShieldException`, for code that's deliberately scanning *multiple candidate files* and expects one bad file to be skipped, not to abort the whole operation:
    - `service_discovery.py:146` (`_looks_like_python_config_module`, `except (FileNotFoundError, OSError): return False`) — heuristic discovery over arbitrary directory contents during `init`/`import`.
    - `doctor.py:239` (`_check_config_source_drift`'s inner loop, `except (FileNotFoundError, OSError): continue`) — scans every "other config source" found in a service directory; a malformed one should be skipped, not blank out the whole health check via `HealthCheck.run()`'s coarser outer catch.
    - `importer.py:386` (`merge_variables_from_other_sources`, `except (FileNotFoundError, OSError): continue`) — same shape, during `import`.
    - `explain.py:143` (`_check_manifest_references`, `except (FileNotFoundError, OSError, ValueError) as e:`) — turns a parse failure into a structured per-manifest `status="error"` entry in the report; without `EnvShieldException` added, that specific manifest's failure would instead propagate out of `explain` entirely rather than showing up as one reported entry among others.
  - All four are low-likelihood in practice (a `.py` file would have to be registered as a deployment manifest, or exist as an "other source" the heuristics scan), but each is a genuine, mechanical regression risk directly created by fixing the root cause, so the fix isn't complete without them.
- **Related, adjacent gaps found in the same investigation (not the same defect, not fixed, recorded separately — see BL-092 and BL-093):** `PythonParser.get_vars` also crashes with an *uncaught* `UnicodeDecodeError` on a non-UTF-8/binary `.py` file (`BL-092`); `KubernetesParser.get_vars` has no `try`/`except` around its own YAML parse at all, unlike `DockerComposeParser` (`BL-093`, lower confidence — not fully live-reproduced end-to-end this pass).
- **Existing test coverage gap:** `envshield/tests/parsers/test_python_parser.py::test_python_parser_sad_path_syntax_error` currently asserts the *buggy* behavior as correct (`variables == set()` on a syntax error, docstring calling it "gracefully" handled) — this test will need to be rewritten to assert `EnvShieldException` is raised, the same pattern as `BL-001`'s `test_generate_config_secret_with_default_stays_secret_str`. No existing test exercises `check --json`/`doctor --json`'s stdout-corruption or false-clean behavior at all.
- **Security check:** `str(SyntaxError)` was confirmed (via direct Python testing) to never include the offending source line's actual text — only the error description, filename, and line number; the line's real content lives in a separate `.text` attribute that `str()` doesn't surface. The current warning message, and any fix that continues using `str(e)`, is therefore already safe from leaking a secret value that happened to sit on the malformed line — this must stay true in the fix (use `str(e)` only, never `e.text`).
- **Proposed fix (smallest safe change, no new abstraction):** In `PythonParser.get_vars`, replace the `print(...); return {} if get_values else set()` branch with `raise EnvShieldException(f"Could not parse '{file_path}': {e}")`, mirroring `DockerComposeParser.get_vars`'s existing message shape exactly. Add `EnvShieldException` to the four call sites' except clauses listed above, preserving their existing skip/report semantics. `check_schema`/`check_result`/`doctor`'s health checks/`setup`/`schema sync` need zero changes — they already do the right thing once the exception exists.
- **Impact:** Breaks the documented machine-readable contract for any CI/tooling consumer; can silently report "clean" when the local config literally cannot be read.
- **Dependencies:** None. Distinct from BL-031 (G3-9), which is the same root cause in G-3's *new, unimplemented* union code — fixing this doesn't fix that, and vice versa.
- **Related:** BL-031, BL-092, BL-093.
- **Implementation (2026-08-26):** `PythonParser.get_vars` (`parsers/_python.py`) now raises `EnvShieldException(f"Could not parse '{file_path}': {e}")` instead of `print()`-ing a warning and returning an empty result — exactly the proposed fix above, mirroring `DockerComposeParser.get_vars`'s existing pattern verbatim. `check_schema`/`check_result`/`doctor`'s health checks/`setup`/`schema sync` needed **zero changes**, confirming the investigation's own prediction. Four companion call sites had `EnvShieldException` added to their except clauses to preserve their existing skip-and-continue semantics: `service_discovery.py::_looks_like_python_config_module`, `doctor.py::_check_config_source_drift`'s inner loop, `importer.py::merge_variables_from_other_sources`, `explain.py::_manifest_references`.
- **Tests:** `test_python_parser.py`'s stale sad-path test rewritten to assert the raise (plus a dedicated test proving the message never echoes a secret-shaped value from the malformed line, and a `TypeError`-branch test). New CLI-level regression tests (`test_cli.py`) proving `check --json`/`doctor --json` stdout is valid single-document JSON on a malformed local `.py` file, and that an empty schema no longer false-cleans. A unit-level `check_result` test (`test_schema_manager.py`). Skip-and-continue regression tests at all four companion sites (`test_service_discovery.py`, `test_doctor.py` — via a mocked discovery step, since the normal path already pre-filters a malformed candidate before doctor's own loop would see it — `test_importer.py`, `test_explain.py`). Full suite: 990/990 passing. `ruff check .`: clean.
- **Live verification (2026-08-26):** Reproduced against the real `envshield` console script in an isolated `/tmp` project, mirroring the original investigation's exact scenario: `check --json`/`doctor --json` with an empty schema and an unterminated-string local `.py` file now return `"success": false`/`"clean": false`, exit 1, and `json.loads(stdout)` succeeds — the false-clean and the stdout corruption are both closed. The Rich (non-`--json`) path prints a clean `Error: Could not parse '...'` line, not a raw traceback. A valid `.py` local file continues to check cleanly, unaffected.
- **Decision:** **Fixed 2026-08-26.** Kept here, not removed, per the charter's own rule that a `CONFIRMED` finding is marked `Decision: Fixed in vX.Y.Z` and kept, so a regression can't quietly reintroduce this.
- **Target:** Done. Not yet released — see CLAUDE.md/AGENTS.md §19 and BACKLOG.md's Part 0 status line.

### BL-092 — `PythonParser.get_vars` crashes uncaught on a non-UTF-8/binary `.py` file
Type: `RELIABILITY` · Evidence: `CONFIRMED` · Priority: **P2**
Source(s): Discovered this pass while investigating `BL-002`'s root cause — a different gap in the same function, not the same defect.
Component: `parsers/_python.py` (`PythonParser.get_vars`)

- **Problem:** `get_vars` opens the file in text mode and calls `f.read()` *inside* its `try: ... except (SyntaxError, TypeError):` block, but `f.read()` can itself raise `UnicodeDecodeError` (a `ValueError` subclass) on invalid-UTF-8 or binary content — a type the except clause doesn't catch.
- **Current behavior:** Live-reproduced directly against `PythonParser`: a `.py` file containing raw non-UTF-8 bytes (`b"\xff\xfe\x00\x01SECRET=1"`) raises an *uncaught* `UnicodeDecodeError` out of `get_vars`. Whether this reaches a user as a raw traceback or a clean CLI error depends entirely on whether the calling command happens to wrap its own call in a broad-enough `except` — the same case-by-case inconsistency `BL-002` is about, just for a different exception type.
- **Expected behavior:** Should raise `EnvShieldException` (or be silently skip-able, for the "opportunistic scan" call sites `BL-002` also identifies), the same as any other unreadable-file case, not propagate as a bare `UnicodeDecodeError`.
- **Evidence/reproduction:** Live-reproduced by calling `PythonParser().get_vars()` directly on a synthetic binary fixture — `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff in position 0: invalid start byte`, uncaught.
- **Impact:** Low-likelihood (requires a genuinely binary/corrupted `.py` file where one is expected), but a real, reproducible crash path distinct from `BL-002`'s false-clean/stdout-corruption symptom.
- **Dependencies:** Same component and likely the same fix window as `BL-002` — whoever implements `BL-002`'s fix should decide whether to widen the except clause to also cover this in the same change, since the mechanism (raise `EnvShieldException` instead of swallowing/crashing) is identical.
- **Related:** BL-002.
- **Implementation (2026-08-26):** Bundled into the same change as `BL-002` — `PythonParser.get_vars`'s except clause widened to `(SyntaxError, TypeError, UnicodeDecodeError)`, all three raising `EnvShieldException(f"Could not parse '{file_path}': {e}")`. `str(UnicodeDecodeError)` describes the failing byte's value/position only (confirmed: `'utf-8' codec can't decode byte 0xff in position 0: invalid start byte`), never any decoded text, so this stays consistent with `BL-002`'s security-check note.
- **Tests:** `test_python_parser.py::test_python_parser_non_utf8_content_raises_instead_of_crashing_uncaught` — a real binary fixture on disk (not mocked, since the failure happens in the actual text-mode read), asserting `EnvShieldException` instead of an uncaught `UnicodeDecodeError`.
- **Live verification (2026-08-26):** Reproduced against the real `envshield` console script: a binary local `.py` file now produces a clean `"error": "Could not parse '...': 'utf-8' codec can't decode byte 0xff..."` in both `check --json` and the Rich path, exit 1 — no uncaught traceback.
- **Decision:** **Fixed 2026-08-26.** Kept here, not removed, per the charter's rule that a `CONFIRMED` finding stays recorded once fixed.
- **Target:** Done. Not yet released — see BACKLOG.md's Part 0 status line (this is a P2, not a Part 0 release blocker itself, but shipped in the same commit as `BL-002`).

### BL-093 — `KubernetesParser.get_vars` has no error handling around its own YAML parse
Type: `RELIABILITY` · Evidence: `NEEDS_EVIDENCE` (confirmed by direct source reading; not fully live-reproduced end-to-end this pass — an attempted live reproduction through `doctor --json` against a malformed Kubernetes manifest did not exercise this code path, for reasons not yet diagnosed) · Priority: **P3**
Source(s): Discovered this pass while comparing `PythonParser`'s error handling against its sibling parsers during `BL-002`'s investigation.
Component: `parsers/_kubernetes.py` (`KubernetesParser.get_vars`)

- **Problem:** Unlike `DockerComposeParser.get_vars`, which wraps `yaml.safe_load` in `try: ... except yaml.YAMLError: raise EnvShieldException(...)`, `KubernetesParser.get_vars` calls `yaml.safe_load_all(f)` with no `try`/`except` around it at all.
- **Current behavior (per source reading only):** A malformed Kubernetes manifest should raise a raw, uncaught `yaml.YAMLError` out of `get_vars`, rather than either `DockerComposeParser`'s friendly `EnvShieldException` or any graceful handling.
- **Expected behavior:** Should match `DockerComposeParser`'s pattern — catch `yaml.YAMLError` and raise `EnvShieldException` with a descriptive message.
- **Evidence/reproduction:** Not yet live-reproduced — an attempt to trigger this through `doctor --json` against a malformed `deployment.yaml` registered in `envshield.yml`'s top-level `manifests:` list did not exercise the "Deployment Manifest" health check at all (the check didn't appear in the `--json` output), suggesting either a manifest-registration or `detect_deployment_format` step short-circuits before reaching `KubernetesParser.get_vars` in that scenario — not yet diagnosed. The source-level gap (no `try`/`except` where `DockerComposeParser` has one) is real and directly confirmed by reading both files side by side.
- **Impact:** Unknown until live-reproduced — could range from "an uncaught traceback on a malformed Kubernetes manifest" (if reachable) to "dead code path, unreachable in practice" (if something upstream always catches malformed YAML first).
- **Dependencies:** None identified yet.
- **Related:** BL-002 (same general theme: parser error-handling consistency; `BL-002` and `BL-092` — the sibling gaps found in the same investigation — were both fixed 2026-08-26, this one was deliberately left open and unimplemented per explicit instruction), BL-003/BL-004's sibling deployment-parser work.
- **Decision:** Open, `NEEDS_EVIDENCE`. Not implemented in the 2026-08-26 `BL-002`/`BL-092` fix pass, exactly as instructed. Needs a live-reproduction pass (why did the doctor --json attempt not reach this path?) before it can be marked `CONFIRMED` or ruled out.
- **Target:** Unscheduled — lower priority than `BL-002`/`BL-092`, and not yet proven live.

### BL-003 (aka RA-P1-6) — Generated TypeScript boolean coercion inverts `"false"`
Type: `GENERATION` (bug) · Evidence: `CONFIRMED` · Priority: **P0** (scoped to `generate --lang typescript`)
Source(s): 2026-08-21 audit; independently live-reproduced with actual runtime execution, not just source reading.
Component: `generator.py:275` (TypeScript `bool` field rendering)

- **Problem:** `bool` fields render as `z.coerce.boolean()`, which follows JavaScript truthiness.
- **Current behavior:** `z.coerce.boolean().parse("false")` returns `true` at runtime. Confirmed by actually running the generated Zod code (`node` + real `zod` package), not just reading the source — every non-empty string, including `"false"` and `"0"`, coerces to `true`; only `""` coerces to `false`.
- **Expected behavior:** Agrees with EnvShield's own validator, which accepts only case-insensitive `"true"`/`"false"`.
- **Evidence/reproduction:** `node runtime_check.mjs` against real `zod`: `"false" -> true`, `"true" -> true`, `"0" -> true`, `"1" -> true`, `"" -> false`, `"no" -> true`, `"False" -> true`.
- **Impact:** Any boolean-typed environment variable explicitly set to `"false"` (a completely ordinary way to disable a flag — e.g. a safety switch like `ALLOW_EMAILS=false`) resolves to `true` in code generated by EnvShield itself. This inverts explicit developer intent, not just a validation miss. It also directly contradicts README's own marketing claim ("typed, **validated** config code") for this field type — validation exists, it just validates to the wrong value.
- **Investigation (2026-08-26):** Independently re-reproduced this pass (`npm install zod` in an isolated scratch directory, real `node` execution against `generator._zod_base_type`'s exact output) — identical results to the original report (`"false"`/`"0"`/`"no"`/`"False"`/`"FALSE"` all -> `true`; only `""` -> `false`). Root cause confirmed unchanged: `generator.py`'s `_zod_base_type` (line 283-284) emits `z.coerce.boolean()` for every `bool` field, which is JS `Boolean(string)` truthiness, not a `"true"`/`"false"` parse. This is an isolated code path — only `_zod_base_type`'s `bool` branch is affected; the Python side (`_python_type_and_extras`'s `bool` branch emits plain `bool`, validated by pydantic's own strict bool coercion, not affected) and TypeScript's own `enum`/`url`/`email` branches use different, unaffected zod builders. Smallest safe fix: replace the `bool` branch with an explicit case-insensitive `"true"`/`"false"` parser (e.g. a `z.string().transform()` or lowercase-then-`z.enum(["true","false"])` pipeline) that rejects anything else, matching `schema_types._BOOL_VALUES`/`validate_value`'s existing case-insensitive `{"true","false"}`-only contract — no new abstraction needed, this is a one-function, ~5-line change. **Testing-infrastructure gap found:** the existing TypeScript generator tests (`test_generator.py`) exercise real `node` for syntax/injection checks but deliberately *never* import `zod` ("without needing zod or any other module installed" — see `_node_check`'s docstring), and the repo has no root `package.json`/`npm`-dependency setup or `node`/`npm` install step in `.github/workflows/ci.yml` at all (bare `node` is simply assumed present on the runner, an existing assumption this finding doesn't introduce). A regression test that actually proves the *fixed* runtime behavior (not just asserts the generated source string) needs real `zod`, which is a genuine test-infrastructure decision — add `zod` as a scoped devDependency plus an `npm ci`/install CI step, or accept a self-contained hand-written re-implementation of the replacement parser's logic in the test (weaker: can't catch a future zod-behavior drift) — not a decision to make silently inside a "smallest fix" pass.
- **Related:** `BL-094` (new, this pass) — the same generator's `int`/`float`/`port` fields also use `z.coerce.number()`, which independently reproduces as diverging from EnvShield's own validator (accepts `"0x10"`, whitespace-padded, and empty-string-as-`0`) — a related but lower-severity manifestation of the same "no single canonical parsing spec shared by the CLI validator and both codegens" architectural gap. Kept separate: unlike `bool`, none of these inputs silently *invert* declared intent, so it doesn't share BL-003's P0 severity or its narrow scope.
- **Dependencies:** None on `BL-004` — different module (`generator.py` vs `scanner.py`), independent code paths, safe to implement in either order or in parallel.
- **Implementation (2026-08-26):** `generator.py::_zod_base_type`'s `bool` branch (was line 283-284) now returns `z.string().transform((s) => s.toLowerCase()).pipe(z.enum(["true", "false"])).transform((s) => s === "true")` instead of `z.coerce.boolean()`. This accepts only a case-insensitive `"true"`/`"false"` (matching `schema_types._BOOL_VALUES`/`validate_value` exactly) and rejects everything else — including non-string input (`undefined`, `null`, a number, a real boolean) — via zod's own type/enum validation, rather than silently coercing. No new file, no new class, no shared helper — the fix is fully localized to this one branch, exactly as scoped. `_zod_default_literal`'s `bool` branch (already emitting a lowercase `true`/`false` JS literal) needed no change, since the new schema's output type is still a plain boolean. The Python codegen (`_python_type_and_extras`'s `bool` branch, pydantic's own strict bool coercion) was not touched — it was already correct and unaffected by this bug.
- **Tests:** `tests/core/test_generator.py`'s `test_generate_typescript_infers_types_from_default_values` (previously asserted the exact buggy `z.coerce.boolean().default(true)` string as "expected" — corrected to assert the new expression). New `TestTypeScriptBooleanCoercionMatchesTheContract` (7 tests): the generated expression renders correctly and `z.coerce.boolean()` is gone from output; `"true"`/`"false"` round-trip correctly (the exact BL-003 scenario — `"false"` must not invert to `true`); case variants (`"True"`/`"FALSE"`/`"TrUe"`/`"fAlSe"`) are accepted; values the CLI validator also rejects (`"0"`, `"1"`, `""`, `"no"`, `"yes"`, whitespace-padded) are rejected, not silently broadened; non-string input (a number, a real boolean, a genuinely absent/`undefined` value on a required field) is rejected; `.default(...)` and `.optional()` still work correctly when chained onto the new expression. All behavioral tests execute the actual generated expression text through real, bare `node` (already an unconditional test dependency — see `_node_check`) against a small, faithful hand-written stand-in for the exact 5 zod methods used (`z.string`, `z.enum`, `.transform`, `.pipe`, `.default`, `.optional`) — not a general zod reimplementation and not a substitute for real-package verification, documented as such directly in the test file.
- **Runtime-verification infrastructure decision (per explicit instruction, not made silently):** proving the fix against the *real* `zod` npm package would require adding `package.json`/npm dependency management and a `node`/`npm` CI setup step, none of which exist in this repo today (Python-only tooling throughout). That infrastructure was not added. Real-zod verification was instead performed ad hoc, outside the repository, in an isolated scratch directory (`npm install zod`, real `node`, the actual current `_zod_base_type` output) — see Live verification below — and is not part of the committed, CI-run test suite.
- **Live verification (2026-08-26):** Ran the actual `generator.generate_config()` TypeScript output for a `bool` field end-to-end through real `node` + real `zod` (v4.4.3, via an isolated ad hoc `npm install zod`, not committed to the repo): `ALLOW_EMAILS=false` — the exact scenario named in Impact above — now resolves to `false` (previously `true`). Full matrix against the real package: `"true"`/`"True"`/`"TrUe"` -> `true`; `"false"`/`"FALSE"` -> `false`; `"0"`/`"1"`/`""`/`"no"`/`"yes"`/`" true"`/`"true "`/`undefined`/`null`/`123`/`true` (a real boolean) -> all rejected. Also ran the full local test suite (997 passed, up from 990 — the 7 new tests) and `ruff check .` (clean).
- **Decision:** **Fixed 2026-08-26, committed** (commit `f478f9f`). Independently re-verified against the current working tree in a post-commit verification pass (same day): the generated `bool` expression re-extracted from live `generator.generate_config()` output and re-run against real `node` + real `zod` produced the identical correct matrix (`"false"` -> `false`, case variants accepted, everything else rejected) -- not merely re-reading the diff.
- **Target:** Done, committed. Not yet released — a further local commit on top of `v4.6.0`, same as `BL-001`/`BL-002`/`BL-004`'s siblings.

### BL-004 (aka RA-P1-7) — `scan`/`scan --staged` fail open on files over 1MB
Type: `SECURITY` · Evidence: `CONFIRMED` · Priority: **P0**
Source(s): 2026-08-21 audit; independently live-reproduced.
Component: `scanner.py` (large-file skip logic)

- **Problem:** Files >1MB are skipped and recorded in `skipped_files`, but do not affect `clean`.
- **Current behavior:** Both `scan --json` and `scan --staged --json` (the exact mode a pre-commit hook or CI gate uses) report `"clean": true` and exit 0 when the only file containing a secret-shaped marker was the one skipped.
- **Expected behavior:** A skipped file should produce a distinct "incomplete" state and a non-zero exit by default in staged/CI contexts.
- **Evidence/reproduction:** Live-reproduced with a synthetic 1.1MB file containing `AWS_SECRET_ACCESS_KEY=SYNTHETIC0000FAKEKEYNOTAREALSECRET1234` on its first line — both `scan` and `scan --staged` returned `clean: true`, exit 0, with the file correctly named in `skipped_files` but that fact having no effect on the pass/fail verdict.
- **Impact:** Any hook/CI gate that checks only the exit code (the standard integration pattern) is fully bypassed by padding a secret-bearing file past 1MB. This is a bypass of the one feature whose entire job is catching this. **Not hypothetical** — confirmed this pass that EnvShield's own installed pre-commit hook (`scanner.py::_generate_pre_commit_hook_content`) is itself exactly this victim: the generated hook script is `envshield scan --staged` followed by `STATUS=$?` / `exit $STATUS`, i.e. the literal exit-code-only pattern this finding describes, wired in by EnvShield's own `hook install` command.
- **Investigation (2026-08-26):** Independently re-reproduced this pass end-to-end (`envshield scan --json`, `--staged --json`, and plain `--staged` against a live 1MB+ synthetic secret file) — identical to the original report: `clean: true`/exit 0 in all three invocations, file correctly named in `skipped_files`/the printed warning, with no effect on the pass/fail verdict. Root cause confirmed unchanged and precisely localized: `scanner.py::scan_result`'s `"clean": not (secrets or undeclared)` (does not reference `skipped`) and `scanner.py::run_scan`'s `found_issues` (only set from `all_secret_findings`/`all_undeclared_findings`) both omit `skipped_large_files` from the pass/fail computation entirely — this was never a computed-then-discarded value, coverage completeness was simply never wired into either presentation's contract. Both `_scan_files`'s two callers (`run_scan`'s Rich path, `scan_result`'s JSON path) share the one root list, so this is a single shared-computation fix, not two.
  **A required fix-scope dependency, found live-reproducing this:** `_scan_files`'s staged-mode branch runs the >1MB size check (line 669) *before* consulting `excluded_files` (computed from `secret_scanning.exclude_files`/`--exclude`), while the non-staged branch already filters exclusions first via `_filter_files` (line 641) and never reaches the size check for an excluded file at all. Live-reproduced this asymmetry directly: a `secret_scanning.exclude_files: ["*.bin"]`-excluded 1MB+ file is correctly absent from `skipped_files` under plain `scan --json .`, but still lands in `skipped_files` under `scan --staged --json` on the identical file/config. This is harmless today only because a skip has no effect on `clean` — the moment BL-004 is fixed to make any skip flip the pass/fail verdict, this ordering gap becomes an immediate new false-failure source for anyone already using `exclude_files`/`--exclude` on a large, deliberately-out-of-scope file in a staged commit. Fixing BL-004 correctly requires reordering the staged-mode check (consult `excluded_files` before the size check, matching the non-staged branch's existing order) as part of the same change — not a separate backlog item, since implementing BL-004 without it introduces a new regression on day one.
  **Design note carried into the fix, not resolved here:** the recorded "Expected behavior" already anticipates a *distinct* incomplete/unknown state, not simply folding `skipped` into `clean` — e.g. `"clean": not (secrets or undeclared)` staying as-is plus a new `"incomplete": bool(skipped)` field, with the CLI wrapper (`cli.py::scan`, the exact site that currently does `if not result["clean"]: raise typer.Exit(code=1)`) checking both. Whether *every* mode (plain interactive `scan`, not just `--staged`/`--json`) should also flip exit code on a skip, or only the hook/CI-facing invocations, is a real product/compatibility tradeoff (an ad hoc `scan .` on a repo with pre-existing large, non-secret, non-excluded assets — media, fixtures — would newly start exiting 1) that this investigation surfaces but does not decide.
- **Dependencies:** The staged-mode exclude/size-check ordering gap above must be fixed in the same change (see Investigation). No dependency on `BL-003` (different module, independent code paths).
- **Design pass (2026-08-26):** Re-confirmed root cause and the exclusion-ordering gap unchanged against current source (both live-reproduced again). Full design deliberation below; nothing implemented — see Decision.
  **Complete affected code path:** `_scan_files` (single shared computation) → `run_scan` (Rich/CLI path, `console.print` + `typer.Exit`) and `scan_result` (JSON path) → `cli.py::scan` (the only caller of either — confirmed no MCP/API/other consumer of `scan_result`/`run_scan` exists anywhere in the codebase) → the installed pre-commit hook (`_generate_pre_commit_hook_content`, which always calls the *plain, non-JSON* `envshield scan --staged` and reads `$?` — this matters directly for the exit-code design below) and any external CI/hook integration.
  **Existing result/exit-code semantics, precisely:** `scan_result` returns `{"clean": not (secrets or undeclared), "secrets": [...], "undeclared_variables": [...], "skipped_files": [...]}` — `skipped_files` is populated but never read by anything downstream. `run_scan` computes `found_issues` from `all_secret_findings`/`all_undeclared_findings` only and calls `raise typer.Exit(code=1)` only if `found_issues`; `skipped_large_files` only drives a printed warning, never the exit path. `cli.py::scan`'s `--json` branch does `if not result["clean"]: raise typer.Exit(code=1)` — same gap, one level up. A structurally different case is already handled correctly and is useful prior art: `_collect_files_to_scan` raises `EnvShieldException` for a typo'd/nonexistent path, which the CLI's existing `except EnvShieldException` handler turns into `{"clean": False, "error": str(e)}` plus exit 1 (`test_scan_json_rejects_a_nonexistent_path_instead_of_reporting_clean`) — proof the "never silently claim clean" invariant is already enforced elsewhere in this same command, just not for a *partial* skip.
  **Exclusion-ordering, confirmed and the correct fix determined:** re-reproduced live — a `secret_scanning.exclude_files`-excluded 1MB+ file is correctly absent from `skipped_files` under non-staged `scan --json .` (exclusions filtered via `_filter_files` before the size check even runs), but still lands in `skipped_files` under `scan --staged --json` on the identical file/config (the staged branch's size check at `_scan_files` line 669 runs *before* the `file_path in excluded_files` branch is ever consulted). The correct ordering is subtler than "just move the check earlier": for a staged file that's excluded, the existing diff-aware logic already has three outcomes -- (a) brand-new (not in `HEAD`): scanned in full *despite* the exclusion, (b) has no new lines: nothing to scan, already `continue`s, (c) has new lines: only that bounded diff is scanned. The size check should apply to the *whole file* only in case (a), where the whole file's content is genuinely what's about to be read -- not in case (c), where only a small, already-bounded diff is scanned and a large *unrelated* excluded file (e.g. a vendored lockfile) shouldn't be flagged "incomplete" over content nobody asked to scan. Concretely: resolve `file_path in excluded_files` and run the existing new-file/no-new-lines/new-lines-only branching *first*; apply `len(content) > MAX_SCANNABLE_SIZE_BYTES` only when the file is not excluded, or is excluded-but-brand-new (i.e. whenever the full file is what's being scanned) -- never when only a diff subset is being scanned.
  **Every silent-drop mechanism found, not just size:** inspecting adjacent code for the same failure class (per the Bug-finding requirement) surfaced two more file-skip sites with *zero* result-model representation at all -- worse than the already-tracked size case, which at least appears in `skipped_files`. `_collect_files_to_scan`'s symlink protection (correct to refuse the symlink -- see its own docstring on the trust-boundary invariant) drops the path into a local `skipped_symlinks` list that's never returned to any caller; the Rich path prints an informational `ℹ️` line but `scan_result`'s `console.quiet = True` suppresses even that, so `--json` shows *nothing at all*. `_scan_single_file`'s `except (IOError, OSError): return [], []` (covering a permission-denied file, a TOCTOU-race symlink caught by `O_NOFOLLOW`, or a race-deleted file) is silent in *both* presentation paths -- not even a console message. Both live-reproduced this pass with a secret-bearing file reachable only through a symlink, and a `chmod 000` file: both report `clean: true`, exit 0, with zero trace in JSON or the terminal. **Important scoping distinction confirmed while reproducing these:** both are reachable only through non-staged `scan` (interactive or `--json`) -- `--staged` mode never calls `_collect_files_to_scan`'s symlink-filtering branch at all (`git_utils.get_staged_files()` returns early), and never calls `_open_for_scan`/hits the `IOError`/`OSError` clause (staged content comes from `git_utils.get_staged_file_content`, i.e. git's own object store, not a raw filesystem read) -- confirmed live: staging a symlink stores only the link-target *path string* as its blob content, so `--staged` mode's handling of a symlinked entry is safe by construction, independent of any EnvShield-side defense. Neither sub-case defeats the pre-commit-hook gate BL-004 itself targets, which is why they're a distinct, lower-priority finding rather than a `BL-004` scope expansion. A third, narrower case (`_get_diff_lines`'s `except Exception: return set()`, which causes an excluded staged file to be silently treated as "no new lines" -- i.e. skipped -- if diff computation itself fails) *is* inside the staged/diff-aware path and confirmed by code reading only, not live-reproduced (a git-internal failure is harder to force deterministically; same evidence-tier reasoning as `BL-093`) -- this one, if ever live-confirmed, would be the more concerning of the three for hook-gate bypass risk specifically. Recorded as new finding **`BL-095`**, not fixed here. This directly answers one of this pass's required questions: **no, the fix must not be scoped only to >1MB files** -- the *field* representing completeness must be named and defined generically (`complete`, not e.g. `all_files_under_size_limit`), even though this pass's actual implementation only populates it from the size-skip case; `BL-095`'s eventual fix should extend the same field's population, not redesign it.
  **Design options evaluated:**
  - **(A) Any skip makes `clean=false` and exit non-zero, unconditionally, in every mode.** Maximally simple and maximally secure against the invariant, but collapses "found a real problem" and "couldn't verify everything" into one bit -- a machine consumer loses the ability to tell them apart (directly fails the "can users distinguish clean from not-fully-scanned" question). Highest false-positive/compatibility cost: an ad hoc `scan .` on a repo with a pre-existing, legitimate large asset nobody ever excluded would newly start exiting 1 on every run, unrelated to anything the developer just did.
  - **(B) A distinct `complete`/`incomplete` field alongside `clean`, `clean` unchanged.** Correct and honest *as a data model* -- `clean` keeps its precise, narrow meaning ("no findings among what was inspected"), and a new field states whether coverage was actually total. Taken completely literally ("preserve clean" as in *also* preserve today's exit-code derivation from `clean` alone) this does **not** by itself close BL-004 -- the hook/CI still read only the exit code, so the fix must also change what drives the exit code, not just add an unused field. The real value of B is distinguishability, not exit-code correctness by itself.
  - **(C) Incompleteness is fatal only for CI/hook-facing modes.** Matches BL-004's own originally-recorded "Expected behavior" line verbatim. Whether this satisfies the stated security invariant depends on *what* varies by mode: the invariant is about the domain result never asserting a false "clean" claim -- a truthful data model (see B) satisfies that in every mode, regardless of what any one mode's exit code does with it. Exit code is a *policy* decision (should automation treat this as a failure), not a truth claim, so it's legitimate for it to vary by context the way `--staged` vs. bare `scan` already differ in other ways (git-ignored-file handling, diff-aware scanning). Lowest false-positive/compatibility cost for interactive use; fully closes the *evidenced* gap as long as the fatal set includes every mode the hook and typical CI usage actually invoke.
  - **(D) Other:** considered and rejected -- a separate "coverage check" command (new CLI surface for one boolean, heavier than reusing `scan`'s own existing touchpoint) and removing the size-skip mechanism entirely (defeats `MAX_SCANNABLE_SIZE_BYTES`'s own documented reason for existing -- memory/cost, not correctness).
  **Recommended semantic model: B + C combined.** `clean` keeps its exact current meaning and computation, unchanged. A new top-level `complete: bool` (`True` iff no eligible file was skipped, for any currently-tracked reason -- today only size) is added to the shared domain result, computed once in `_scan_files`/`scan_result` (not duplicated per presentation path -- this is exactly the "two independent computations that could quietly drift" pattern `_scan_files`'s own docstring already exists to prevent). The exit-code/pass-fail *policy* -- not the JSON fields -- is what varies by mode: fatal (`not clean or not complete`) whenever `--json` is used (JSON output is inherently a machine/automation signal) **or** whenever `staged_only` is true, in *both* the Rich and JSON paths -- this second condition is not optional, because the installed hook invokes plain, non-JSON `envshield scan --staged`, and a fix that only changed the `--json` branch's exit code would leave the actually-evidenced hook-bypass completely open. A bare interactive `scan` (no `--staged`, no `--json`) keeps today's behavior exactly: an informational warning, exit 0. This directly answers the pass's specific questions: `clean=true` should remain *possible* when eligible content wasn't inspected (it's a narrow, honest claim about findings only, not an overall verdict) as long as `complete` is also surfaced and the exit code/any single top-level "did this pass" signal accounts for both; yes, "incomplete" should be first-class (a named boolean, not inferred from list length by every consumer); no, exit code and the semantic result should not be forced to be the same thing -- domain truth stays constant across modes, policy legitimately varies; yes, the distinction belongs in the domain result (`scanner.py`), not presentation, and the recommended design keeps it that way.
  **Fundamental model-capacity check (per explicit instruction to state this before implementation, if true):** the current result model does **not** fundamentally lack the capacity to represent "clean but incomplete" -- `skipped_files` already carries the exact raw signal needed; the bug is purely that nothing downstream ever converts `skipped_files` non-empty into an honest top-level verdict. No redesign is needed, only an additive field plus a policy wire-up.
  **Recommended CLI/JSON/exit-code contract:** `scan_result` gains `"complete": bool` alongside the four existing keys (`skipped_files`'s own existing shape -- a flat list of path strings -- stays unchanged, for backward compatibility with any existing consumer that already reads it; a future richer per-reason shape is `BL-095`'s own deferred design question, not resolved here). `cli.py::scan`'s `--json` branch's exit condition becomes "`not result["clean"] or not result["complete"]`". `run_scan`'s exit condition gains the equivalent for `staged_only` specifically (bare `--staged` must also become fatal on incompleteness, matching the hook's literal invocation); a bare, non-staged `scan` keeps exit 0 with only the existing informational warning.
  **Proposed implementation boundary:** `scanner.py::_scan_files` (reorder the staged-mode exclusion/size-check as described above -- no return-shape change needed, it already returns `skipped_large_files`), `scanner.py::scan_result` (add the `complete` key), `scanner.py::run_scan` (extend the exit condition for `staged_only`), `cli.py::scan` (extend the `--json` exit condition). No new file, class, or framework -- an additive field and two conditional-expression changes, consistent with "use the existing architecture."
  **Required regression tests (design-level, not yet written):** bare `--staged` with a skipped file now exits 1 (the literal hook-invocation shape); `--staged --json` and plain `--json` (non-staged) both exit 1 and show `"complete": false`; a bare non-staged, non-JSON `scan` with a skipped file still exits 0 (explicit no-regression coverage for the preserved interactive UX); an `exclude_files`-excluded oversized file no longer appears in `skipped_files` under `--staged` (parity with non-staged); a brand-new (not in `HEAD`), excluded, oversized staged file *is* still skipped/flagged (size check still applies when the whole file is being scanned); an excluded staged file with a small diff and a large *total* file size is **not** flagged incomplete (size check correctly not applied to a bounded diff scan); the existing `test_scan_reports_skipped_large_files` needs its `exit_code == 0` assertion corrected (same stale-test pattern already fixed for `BL-002`/`BL-003`).
  **Backwards-compatibility implications:** `clean`'s meaning and value are unchanged in every case -- zero risk to any consumer reading only that field. `skipped_files`'s shape is unchanged. The only breaking-by-design behavior change is exit code for `--staged` and/or `--json` invocations that have a skip -- exactly BL-004's own intended fix, not a side effect. A bare interactive `scan` sees no exit-code change at all.
- **Implementation (2026-08-26):** `_scan_files`'s staged-mode branch reordered exactly as designed -- exclusion/diff-aware resolution now runs before the size check, and the size check only fires when the whole file's content is actually about to be read (non-excluded, or excluded-but-brand-new); an excluded file with a bounded diff is never sized against its total. `scan_result` gained `"complete": not skipped` (additive; `clean`, `secrets`, `undeclared_variables`, `skipped_files` all byte-for-byte unchanged in meaning and shape). `run_scan` now derives `fatal_incomplete = bool(skipped_large_files) and staged_only`; when true, it falls through to the existing `staged_only` "Commit aborted" + `typer.Exit(1)` path even with zero actual findings. When incompleteness isn't fatal, the old "✓ No issues found... secure and compliant!" message is now printed *only* when nothing was skipped -- an honest "⚠ Scan finished, but coverage was incomplete" message replaces it otherwise, in every mode, so the printed claim is never false even where the exit code doesn't change. `cli.py::scan`'s `--json` branch's exit condition became `not result["clean"] or not result["complete"]`, unconditionally (not gated on `staged_only`, per design -- JSON is inherently machine-facing). The installed pre-commit hook (`_generate_pre_commit_hook_content`) required **zero changes** -- confirmed by installing a real hook via `envshield hook install --yes` in a live git repo and committing an oversized secret-bearing file through it end-to-end (see Live verification).
  **A contradiction found in this entry's own prior design-pass text, resolved rather than silently changed:** the "Required regression tests" bullet above contains two mutually inconsistent statements about the exact same test -- it says both "a bare non-staged, non-JSON `scan` with a skipped file still exits 0" *and*, in the same sentence, that `test_scan_reports_skipped_large_files` "needs its `exit_code == 0` assertion corrected." Those can't both be right for that test, which uses plain, non-staged `scan`. The approved semantic model (B+C, stated as the actual recommendation and restated in the Backwards-compatibility line above: "A bare interactive `scan` sees no exit-code change at all") is unambiguous and stated three separate times elsewhere in this same entry; the single contradicting clause was a drafting slip made while pattern-matching against `BL-002`/`BL-003`'s different situation (a genuinely wrong test), not a real second design intent. Resolved per the dominant, repeated statement: `test_scan_reports_skipped_large_files`'s `exit_code == 0` assertion is correct and was left unchanged -- the test was instead strengthened to assert the new honest message text appears and the old "secure and compliant" claim does not, which is the part of it that actually needed updating.
- **Tests:** `test_scan_reports_skipped_large_files` (existing, strengthened -- new message assertions, exit code correctly left at `0`). New `TestScanCompletenessContract` (12 tests) in `test_scanner_compliance.py`: `complete=True`/`complete=False` for a clean vs. an oversized-file result; bare `--staged` and `--staged --json` both exit 1 with `complete=false` on a skip (the literal hook-invocation shape, plus the JSON form); non-staged `--json` exits 1 on incompleteness; a bare non-staged interactive scan retains exit 0 (explicit no-regression case); an excluded oversized file with a small diff is *not* flagged incomplete; an excluded-but-brand-new oversized file *is*; an excluded oversized file with a diff that adds no new lines (a pure deletion, not "identical content re-staged" -- git doesn't even list the latter as staged at all, confirmed while writing this test) is not flagged; multiple skipped files are all reported; JSON output is a single valid document when incomplete; skipped-file reporting contains no secret/file content (only a bare path string). `test_cli.py::test_scan_json_reports_clean_state` (existing, exact-dict-equality assertion) updated to include the new additive `complete: True` key.
- **Live verification (2026-08-26):** Full suite 1009/1009 passed (was 997), `ruff check .` clean. End-to-end: installed a real pre-commit hook via `envshield hook install --yes` in an isolated git repo, staged and committed an oversized secret-bearing file -- commit correctly **blocked** (exit 1, "Commit aborted"), with zero changes to the generated hook script itself (`envshield scan --staged` / `STATUS=$?` / `exit $STATUS`, byte-for-byte identical to before this fix). A subsequent ordinary clean commit through the same installed hook still succeeded normally (no regression).
- **New findings from this implementation pass:** None beyond the design-note contradiction above (a documentation slip in this same entry, not a code defect) -- no new genuinely distinct scanner bug was discovered while implementing.
- **Decision:** **Fixed 2026-08-26, committed** (commit `134ef7e`). Independently re-verified against the current working tree in a post-commit verification pass (same day): re-ran the real installed-hook end-to-end test (oversized secret-bearing staged file blocked, exit 1), the non-staged/`--json`/`--staged --json` exit-code matrix, and both exclusion-ordering sub-cases (excluded-with-bounded-diff not flagged; excluded-but-brand-new flagged) -- all reproduced identically to the implementation pass, not merely re-read from the diff.
- **Target:** Done, committed. Not yet released — a further local commit on top of `v4.6.0`, same as `BL-001`/`BL-002`/`BL-003`'s siblings. `BL-095` remains open, deliberately not implemented as part of this fix.

---

## Part 1 — Immediate / high-priority correctness (not P0/P1 in the original report, but confirmed and should not wait for general triage)

### BL-005 (aka EB-3) — `schema sync --check` false-cleans against a Python local file genuinely missing a declared variable
Type: `RELIABILITY` / `SECURITY` (false-clean, hook-bypass) · Evidence: `CONFIRMED` · Priority: **P0** (elevated 2026-08-26 — see Investigation below)
Source(s): This engagement's own G-3 investigation (flagged repeatedly across the spec's revisions); independently live-reproduced this pass; re-verified and substantially deepened in a dedicated 2026-08-26 investigation.

Component: `doctor.py::_check_example_file_sync` (the sole cause) — called directly by `schema sync --check` (`cli.py::schema_sync`, line 820), by `doctor`'s own "Template Sync" health check, and (unconditionally, for every service regardless of local_file type) by the installed pre-commit hook (`scanner.py::_generate_pre_commit_hook_content`, line 1002-1004).

- **Problem:** `--check` delegates entirely to `_check_example_file_sync`, which unconditionally returns `True` for any `.py` local_file without inspecting actual variable coverage.
- **Current behavior:** Live-reproduced: a schema with `FIELD_PRESENT` and `FIELD_MISSING`, a local `.py` file declaring only `FIELD_PRESENT` — `schema sync --check` prints `✓` and exits 0 despite `FIELD_MISSING` being completely absent. Also confirmed `--json` isn't a supported flag on this subcommand at all (inconsistent with its siblings).
- **Expected behavior:** Should fail (non-zero exit) when the local file is genuinely out of sync with the schema.
- **Evidence/reproduction:** Live-reproduced in an isolated fixture this pass.
- **Investigation (2026-08-26), answering each required question:**
  1. **Exact triggering condition:** `_check_example_file_sync` checks `local_file.endswith(".py")` and returns `(True, "...has no separate template file...")` immediately, before ever loading `schema.keys()` or the local file's actual declared variables. Any `.py`-local_file service triggers this unconditionally.
  2. **Required/optional/defaulted field behavior:** No distinction exists — confirmed live with a local file declaring **zero** of four schema variables (one required, one `requiredIf`-conditional, one defaulted, one plain) — still reports `✓`. The short-circuit fires before any field is ever inspected, so field type is irrelevant.
  3. **`.py` vs. other source types:** Isolated to `.py`. The identical schema-drift scenario against a real `.env`/`.env.example` pair correctly reports `✗ Missing from '.env.example': FIELD_NEW_REQUIRED`, exit 1 — live-confirmed side by side with the `.py` case.
  4. **`schema sync` vs. `schema sync --check`:** Materially different. Plain `schema sync` (write mode, no `--check`) correctly delegates to `_sync_python_local_file` and **does** write missing variables directly into the `.py` file — live-confirmed it added all 4 missing variables. The bug is isolated to the read-only `--check`/health-check reporting path; the underlying write logic is correct.
  5. **Human vs. JSON output:** `schema sync --check --json` does not exist (`No such option: --json`, exit 2) — confirmed live. This subcommand has no machine-readable output at all, unlike `check --json`/`doctor --json`.
  6. **Exit codes:** `0` (false success) for the `.py` case in every current entry point tested; `1` for the equivalent non-`.py` case.
  7. **Can a CI/pre-commit/machine consumer receive a false success?** Yes — confirmed two ways. (a) `schema sync --check`'s exit code alone is `0` regardless of actual drift, and it has no JSON alternative to fall back on. (b) **Directly proved via a real installed hook and a real `git commit`**: with a `.py`-local_file service, a staged schema change that adds a new required variable — with the local `.py` file deliberately left un-updated — commits successfully through the actual installed pre-commit hook (`envshield hook install --yes`, real `git commit`), printing `✓ 'config.py' has no separate template file...` and never setting `STATUS=1`. This is not a standalone-command edge case; it is a proven bypass of the same installed hook `BL-004` was fixed to protect.
  8. **Intentional or accidental artifact?** The code comment's own reasoning ("it IS the contract... 'Local Environment Sync' already checks it") is **true and correct** in `doctor`'s own multi-check context, where a companion "Local Environment Sync" check runs alongside "Template Sync" and does catch real drift (live-confirmed: `doctor` on the same zero-coverage fixture correctly fails with `✗ Local Environment Sync: Missing variables: ...`). It is **false/misleading** when the same function is reused standalone by `schema sync --check`, which never runs any companion check — confirmed by reading `cli.py::schema_sync`'s `--check` branch, which calls `_check_example_file_sync` and nothing else. This is an accidental implementation artifact from reusing a function outside the context its own reasoning depends on, not a deliberate design decision for the `--check` path specifically.
  9. **Does the same short-circuit pattern exist elsewhere?** No — searched every `.py`-conditional branch reachable from the affected commands (`doctor.py`, `schema_manager.py`, `setup_manager.py`, `importer.py`, `scanner.py`). The others are either legitimate scope-narrowing (a check that genuinely doesn't apply to non-`.py` sources) or correct dispatch logic (choosing which parser/writer to use) — none silently claim success without inspection the way this one does.
  10. **Same failure class as BL-002/BL-004, or narrower?** The same class — a machine/hook-facing check answering "is this in sync" that can be trivially, unconditionally false-positive for a fully-supported, documented configuration (a `.py`-local_file service) — and, per (7), now proven to defeat the identical installed pre-commit hook `BL-004` was fixed to protect. Narrower only in blast radius (scoped to `.py`-local_file services, not every scan), not in mechanism or proven severity.
  11. **Release blocker under the current charter?** See the session's own report accompanying this investigation for the full reasoning; the evidence above independently satisfies the charter's "can this produce a silent false-clean" release-blocking question, with a live-proven hook bypass exactly analogous to `BL-004`'s own evidence.
- **Impact:** A trivially-triggered, live-proven bypass of the installed pre-commit hook's schema-drift protection for any `.py`-local_file service — no special construction needed (unlike most `BL-095` sub-cases), just an ordinary schema edit without updating the local file, which is an everyday development mistake, not an edge case.
- **Dependencies:** None on `BL-001`–`BL-004` (different function, different command). Not fixed by any of those four.
- **Decision:** Open. Not implemented this pass, per explicit instruction (investigation only). Priority elevated from P1 to P0 based on this investigation's evidence — see Investigation above for the full justification.
- **Target:** Treat as a release-blocking candidate alongside `BL-001`–`BL-004`, pending the user's explicit release-scope decision (see the session's own report).

### BL-095 — `scan` silently drops a symlinked, unreadable, or diff-failed file from coverage with zero representation in any output, including the new `complete` field
Type: `SECURITY` · Evidence: `CONFIRMED` (all 3 sub-cases now directly tested — 2 via real CLI, 1 via direct function-level testing with a realistic simulated failure) · Priority: **P1**
Source(s): Discovered while investigating `BL-004`, inspecting adjacent scanner code for the same "silent skip" failure class; re-verified and deepened in a dedicated 2026-08-26 investigation, now that `BL-004`'s `complete` field exists to test against.
Component: `scanner.py::_collect_files_to_scan` (symlink filtering), `scanner.py::_scan_single_file` (`except (IOError, OSError)`), `scanner.py::_get_diff_lines` (`except Exception`)

Three independent skip mechanisms exist in `scanner.py` besides the already-tracked `>1MB` case, and none of them are recorded anywhere in either output format — worse than `BL-004`'s own size-skip, which at least appears in `skipped_files`.

1. **Symlink protection.** `_collect_files_to_scan` correctly refuses to follow a symlink (the right security behavior — see its own docstring on the trust boundary) but drops the path into a local `skipped_symlinks` list that's never returned to any caller. The Rich path prints an informational `ℹ️` line; `scan_result`'s `console.quiet = True` suppresses even that, so `--json` shows nothing at all. Live-reproduced: a project whose only content is reachable via a symlink to a file outside the scanned directory containing a secret-shaped marker — `scan --json .` reports `"clean": true`, exit 0, `"skipped_files": []`.
2. **Unreadable file.** `_scan_single_file`'s `except (IOError, OSError): return [], []` silently swallows a permission-denied file (or a race-deleted one, or a TOCTOU-race symlink caught by `O_NOFOLLOW`). No console message in either path — more silent than case 1. Live-reproduced: a `chmod 000` file containing a secret-shaped marker — `scan --json .` and plain `scan .` both report clean, exit 0, with zero trace anywhere, not even a warning line.
3. **Diff-computation failure on an excluded staged file.** `_get_diff_lines`'s `except Exception: return set()` causes `_scan_files`'s staged-mode branch to treat the file as having "no new lines" (`continue`s, i.e. skips it) whenever computing the diff itself throws. **Upgraded from code-reading-only to directly tested (2026-08-26):** patched `git_utils.get_head_file_content` to raise a `RuntimeError` (simulating a git-internal failure not caught by that function's own narrower `(subprocess.CalledProcessError, FileNotFoundError)` handler — e.g. a `PermissionError` if the `git` binary itself became non-executable, or a `MemoryError`/pathological-diff failure) and called the real `_scan_files`/`scan_result` end-to-end against a real staged, excluded, secret-bearing file: the secret was not found, and the file never appeared in `skipped_files`. This is now a directly-tested code-behavior fact, not an inference — but the realistic trigger conditions remain narrower than sub-cases 1/2 (which fire on *any* symlink or *any* permission-denied file with no special construction): `get_head_file_content`'s own try/except already absorbs the two ordinary git-failure modes (a non-existent-in-HEAD file, a generic git error), so this path requires a less common failure class. A true end-to-end CLI/hook reproduction (vs. this direct, realistic function-level test) was not attempted — it would require artificially breaking `git` itself in a way that's invasive and environment-dependent.

- **Scoping distinction (important for severity):** sub-cases 1 and 2 are reachable only through non-staged `scan` (interactive or `--json`) — confirmed live that `--staged` mode never reaches either code path (`git_utils.get_staged_files()`/`get_staged_file_content()` read from git's own object store, not the raw filesystem; staging a symlink stores only the link-target *path string* as its blob content, so it can't leak the target's real content even without any EnvShield-side defense). Neither sub-case defeats the installed pre-commit hook's gate. Sub-case 3 *is* inside the staged/diff-aware path — now directly tested (see above) to confirm it *would* defeat the hook's gate if triggered, though its realistic trigger conditions are narrower. This asymmetry (1/2: trivial trigger, no hook risk; 3: narrow trigger, real hook risk) is the main argument for eventually splitting sub-case 3 into its own entry; kept grouped here for now since all three share one remediation shape (extend `_scan_files`'s completeness tracking) and were discovered together — a judgment call, not a settled decision.
- **`complete` field does NOT represent any of these three conditions (2026-08-26, directly tested against the now-shipped `BL-004` fix):** all three sub-cases were re-run against the current `scan_result()`, which now includes `complete`. Live result for all three, identically: `"clean": true, "complete": true` — a secret-bearing file is present, was never inspected, and the `complete` field (whose entire purpose is to answer "was everything eligible actually inspected") reports `true` anyway, because it's computed only from `skipped_large_files`, which none of these three mechanisms populate. This directly and concretely confirms `BL-004`'s fix does not extend to this finding, and that a caller checking both `clean` and `complete` — the exact combination `BL-004`'s own design recommends for full trust — still gets a false all-clear here.
- **Impact:** Violates the exact same invariant `BL-004` protects ("never claim a scan is clean when eligible content was not inspected") for two additional, confirmed reasons beyond size, and for a third, now directly-tested reason. Non-staged `scan --json` is a real, if less severe, threat surface — e.g. a periodic CI job scanning a whole repo (not gating one commit) would also silently miss a symlinked or permission-denied secret file, and would report `complete: true` while doing so.
- **Dependencies:** Directly related to `BL-004`'s own fix — the `complete: bool` field's *meaning* was deliberately kept generic ("was every eligible file actually inspected," not "was every file under the size limit") specifically so this finding's eventual fix can extend the same field's population instead of redesigning it, confirmed still the right shape by this pass's testing. Not implemented as part of `BL-004`'s fix — different functions, different code paths, out of that fix's explicit scope.
- **Decision:** Open, backlog. Not implemented — recorded per `BL-004`'s design pass, deepened by this pass's investigation, per explicit instruction not to fix incidentally-discovered issues. `BL-004` itself was implemented and fixed 2026-08-26 without touching any of this entry's three named code sites (`_collect_files_to_scan`, `_scan_single_file`, `_get_diff_lines` are all unchanged in that fix's diff) — confirmed, not just assumed.
- **Target:** `BL-004`'s `"complete": not skipped` field exists in shipped, committed code, exactly as generically named/scoped as this entry's own Dependencies line anticipated — this finding's eventual fix should extend that field's population (e.g. folding `skipped_symlinks`/an unreadable-file list into the same `skipped` computation `_scan_files` already returns) rather than introducing a second, parallel completeness signal.

### BL-090 — `AGENTS.md` has diverged from `CLAUDE.md`
Type: `DOCUMENTATION` · Evidence: `CONFIRMED` · Priority: **P2**
Source(s): Discovered this pass while inspecting the repo's existing documentation conventions.
Component: `AGENTS.md`

- **Problem:** `AGENTS.md` (the generic/Codex-equivalent charter, structurally parallel to `CLAUDE.md`) is dated 2026-08-18 and still references "Last revised: 2026-08-10" as its most recent update — missing the entire post-v1 direction-setting pass, Phase 0C/2C completion detail, the 2026-08-21 audit, and the external secret-provider-references section that `CLAUDE.md` already has.
- **Current behavior:** `diff AGENTS.md CLAUDE.md` shows real, substantive divergence, not just a section-numbering difference (`AGENTS.md` has its own "Codex behavior" section where `CLAUDE.md` has "Claude behavior," confirming they're deliberately parallel, not accidentally duplicated).
- **Expected behavior:** Both should stay in sync on shared charter content (identity, positioning, engineering/security principles, current-phase status), diverging only where genuinely tool-specific.
- **Evidence/reproduction:** Direct `diff` this pass.
- **Impact:** Any AI coding agent other than Claude working on this repo is currently operating from a five-day-stale picture of the product's own charter and current phase.
- **Dependencies:** None.
- **Decision:** **Fixed 2026-08-26.** `AGENTS.md` brought back to full content parity with `CLAUDE.md` — the "Last revised" pointer, the §2 external secret-provider-references subsection, and the entire §19 current-phase block (including the BL-001–BL-004 correction below) were synced. Re-diffed after the fix: the only remaining differences are the intentional `Codex`/`Claude` tool-naming substitutions throughout §18/§22/§23, which should stay divergent.
- **Target:** Done. Kept here, not deleted, as the record of what was found and fixed.

### BL-091 — Charter's `v4.6.0` "Release status" note misstates the actual tag date and commit count
Type: `DOCUMENTATION` · Evidence: `CONFIRMED` · Priority: **P3**
Source(s): Discovered this pass while establishing the "Versioning and Release Cadence" policy and checking git history/tags as that task required.
Component: `CLAUDE.md`/`AGENTS.md` §19's "Release status (2026-08-19; do not disturb without explicit instruction)" note.

- **Problem:** That note states `v4.6.0` "was tagged and pushed to GitHub" around 2026-08-19, with "five further engineering fixes (3.2/3.3/3.5, 3.4, 3.6, 3.7, 3.9) committed locally on top of v4.6.0" as of that date.
- **Current behavior:** `v4.6.0` is a lightweight tag pointing at commit `332e692` ("docs: finish repositioning around the configuration-contract thesis for 4.6.0"), dated **2026-08-23 14:59:32 +0600** — four days after the note's own "2026-08-19" date. A tag cannot predate the commit it points to, so the "2026-08-19" tagging date is definitively wrong, not just imprecise. At the moment this was discovered (i.e. counting only this repository's state up through `bb3c10f`, the commit already at `HEAD` when this session began, and excluding the two commits this session itself just added for `BL-001`), `git log v4.6.0..bb3c10f` showed exactly **one** commit ahead of the tag (`bb3c10f` itself), not "five further engineering fixes." Those five labels ("3.2/3.3/3.5, 3.4, 3.6, 3.7, 3.9") don't match anything in `git log`, then or now, and appear to be the same unresolved numbering scheme §19 already flags elsewhere ("3.8"/"3.10 ... their exact original content could not be located").
- **Expected behavior:** The note's factual claims (tag date, what's committed-but-unreleased) should match git history, or be explicitly marked as unverified/superseded.
- **Evidence/reproduction:** `git cat-file -t v4.6.0` → `commit`; `git log -1 --format="%H %ci %s" v4.6.0` → `332e692... 2026-08-23 14:59:32 +0600`; `git log v4.6.0..HEAD` enumerated at the time of this finding.
- **Impact:** Low — doesn't affect current behavior or security, and the note's core instruction ("do not disturb without explicit instruction," "PyPI still shows 4.5.1") isn't necessarily wrong, just its supporting date/commit-count detail. But it's exactly the kind of unverified-claim drift the 2026-08-21 audit's own "GO WITH QUALIFICATIONS" line taught this project not to let stand unexamined (see `BL-001`–`BL-004`'s own history).
- **Dependencies:** None. Distinct from the already-flagged 3.8/3.10 numbering loose end (same symptom, but that one is about deferred backlog items, not this tagging-date claim).
- **Decision:** Open, not fixed this pass — out of scope for the versioning-policy task that discovered it, and the note is explicitly protected ("do not disturb without explicit instruction") pending the user's direction on whether/how to correct it. The live PyPI-version claim (`4.5.1`) was not re-verified this pass either — this finding is about the tag-date/commit-count claim specifically, not a claim that PyPI's version is also wrong.
- **Target:** Whenever the release-status note is next touched with explicit instruction — likely alongside an actual v4.6.1 (or later) release, per the new "Versioning and Release Cadence" policy.

---

## Part 2 — Confirmed findings (non-blocking)

### BL-008 (aka RA-P2-9) — Compose `env_file` reads outside the project boundary
Type: `SECURITY` · Evidence: `CONFIRMED` · Priority: P2
Source(s): 2026-08-21 audit; live-reproduced.
Component: `parsers/_docker_compose.py`

- **Problem:** `env_file: ../outside.env` is read without a project-boundary check.
- **Current behavior:** Live-reproduced: a synthetic value in a file one directory above the project root was successfully read and validated as satisfying the schema.
- **Expected behavior:** Validated the same way `envshield.yml` paths already are (`_ensure_within_project`, realpath-based).
- **Impact:** A committed, untrusted manifest can make EnvShield read an arbitrary file outside the project.
- **Decision:** Open, backlog.
- **Related:** BL-010 (a different trust-boundary gap in the same general area — extends — kept separate, different code path and different failure mode).

### BL-010 (aka RA-P2-11) — `extends` cycle detection is lexical, not physical
Type: `SECURITY` / `RELIABILITY` · Evidence: `CONFIRMED` · Priority: P2
Source(s): 2026-08-21 audit; live-reproduced, including its actual practical severity.
Component: `config/manager.py::_load_schema_file`, `config/manager.py::resolve_field_provenance` (duplicated logic, both affected)

- **Problem:** `real_path = os.path.abspath(schema_path)` — despite the variable's name, this does not resolve symlinks, unlike `_ensure_within_project`'s already-hardened containment check.
- **Current behavior:** Live-reproduced with a self-referencing symlink (`real_dir/self -> real_dir`, `extends = "self/x.schema.toml"`): the lexical cycle detector never fires. Confirmed identical behavior in both `_load_schema_file` (via `check`) and `resolve_field_provenance` (via `explain`).
- **Practical severity correction from the original report:** does **not** hang or exhaust the stack. The growing `self/self/self/...` path is bounded by the *operating system's* own symlink-resolution depth limit (`MAXSYMLINKS`/`ELOOP`, ~40 hops on Linux), at which point `os.path.exists()` starts returning `False` and a normal (if extremely garbled — dozens of repeated `self/` segments) `SchemaNotFoundError` is raised. Terminates in well under a second.
- **Expected behavior:** Cycle detection should use `os.path.realpath`, matching `_ensure_within_project`'s already-correct pattern; the two duplicated implementations should likely share one helper so a future fix can't patch only one.
- **Decision:** Open, backlog. Not release-blocking — real but bounded, and the practical consequence today is a bad error message, not resource exhaustion.

### BL-011 (aka RA-P2-12, grouped) — Deployment-parser accuracy gaps (Compose + Kubernetes)
Type: `DEPLOYMENT` · Evidence: `CONFIRMED` (all five sub-cases live-reproduced) · Priority: P2
Source(s): 2026-08-21 audit; every sub-case independently live-reproduced this pass.
Component: `parsers/_docker_compose.py`, `parsers/_kubernetes.py`

Grouped as one backlog item per the instruction to avoid five near-duplicate entries — kept as distinct, independently-closeable sub-items underneath, since each has its own fix location and its own regression test.

1. **Compose `${VAR-default}` (no colon) is not resolved to the default.** Live-reproduced precisely: `${VAR:-default2}` correctly resolves and validates as `"default2"`; `${VAR-default1}` does not match `_INTERPOLATION_RE` (`^\$\{([A-Za-z_][A-Za-z0-9_]*)(:-(.*))?\}$`) at all and falls through to the raw, un-interpolated literal text, which then gets flagged `invalid` against a schema pattern the real default would have passed. **Worse than "unsupported": produces an actively misleading validation error on syntactically valid Compose**, not a clean "unrecognized syntax" signal.
2. **Kubernetes `envFrom.prefix` is ignored.** Live-reproduced: a ConfigMap key `SYNTHETIC_KEY` with `prefix: "PREFIXED_"` in the manifest — EnvShield reports `PREFIXED_SYNTHETIC_KEY` (what the container actually receives) as **missing**, and `SYNTHETIC_KEY` (the un-prefixed name, never actually delivered to the container) as **extra**. A compound, two-sided error from one root cause.
3. **Kubernetes `secretKeyRef`/`configMapKeyRef` matched by `env[].name`, not the referenced key.** Live-reproduced: a field satisfied purely because its schema name matched the env entry's `name:`, while its `secretKeyRef.key:` pointed at an entirely different Secret key.
4. **Kubernetes `initContainers` are not inspected.** Live-reproduced: a variable set only on an `initContainers` entry is reported fully missing.
5. **A Kubernetes manifest with no supported pod template gives an unhelpful generic all-missing result.** Live-reproduced with a ConfigMap-only manifest (no Deployment/StatefulSet/etc.) — indistinguishable output from a real, genuinely-empty container spec; no message clarifies that there was nothing to check.

- Decision: Open, backlog. Not release-blocking (these are within already-documented "supported patterns" territory per the charter, not silent secret exposure or false-clean-on-a-schema-required-field) — but real, and #1/#2/#3 produce actively misleading results, not just gaps, which should weigh toward earlier fixing than pure "missing feature" items.

### BL-094 — Generated TypeScript `int`/`float`/`port` coercion diverges from EnvShield's own validator
Type: `GENERATION` · Evidence: `CONFIRMED` · Priority: P2
Source(s): Discovered this pass while investigating `BL-003`, checking whether boolean coercion was an isolated bug or a broader failure class.
Component: `generator.py::_zod_base_type` (TypeScript `int`/`float`/`port` field rendering — `z.coerce.number()` / `z.coerce.number().min(1).max(65535)`)

- **Problem:** Like `BL-003`'s `z.coerce.boolean()`, these fields render as zod's `.coerce.number()`, which is JS's `Number(string)` coercion — permissive in ways EnvShield's own `schema_types.validate_value` (`int`: `re.fullmatch(r"-?\d+", value)`; `port`: `re.fullmatch(r"\d+", value)` plus a 1-65535 range) is not.
- **Evidence/reproduction:** Live-reproduced with real `node` + real `zod` this pass: `z.coerce.number().parse("0x10")` -> `16` (hex reinterpreted, Python validator: invalid), `.parse("  42  ")` -> `42` (whitespace-tolerant, Python validator: invalid), `.parse("")` -> `0` (empty string silently becomes zero, same underlying `Number("")===0` mechanism as `BL-003`'s `""` -> `false`), `.parse("1e3")` -> `1000` (exponential notation accepted, Python validator: invalid).
- **Impact:** Lower severity than `BL-003` — none of these *invert* declared intent the way `"false"` -> `true` does, they're over-permissive rather than semantically backwards. But a TypeScript consumer of generated config can accept a value (e.g. a hex-looking port number, or whitespace-padded input from a misconfigured shell) that EnvShield's own `check`/`doctor` would reject as invalid — the same declared contract enforced inconsistently depending on which generated language reads it.
- **Deeper architectural note:** together with `BL-003`, this shows there is no single canonical parsing/validation spec shared across the three independent implementations of "what does `type = bool`/`int`/`float`/`port` mean" — the CLI's own `schema_types.validate_value`, the Python codegen (`_python_type_and_extras`, unaffected — pydantic's strict typing matches the CLI validator), and the TypeScript codegen (`_zod_base_type`, affected on all four non-string/enum/url/email types). Not a case for a new shared abstraction today — none of `schema_types.py`'s existing "structured model" justifications (§5) apply yet — but worth tracking if a third codegen target is ever added.
- **Dependencies:** None. Independent of `BL-003`'s fix — same file, sibling function, but a different branch (`bool` vs `int`/`float`/`port`), so `BL-003` can be fixed without touching this.
- **Decision:** Open, backlog. Not release-blocking — recorded per `BL-003`'s investigation, not implemented.
- **Target:** Whenever TypeScript codegen's numeric coercion is next revisited — no urgency independent of `BL-003`.

### BL-020 (aka G-1) — `import`'s Python heuristic destroys config on mixed literal/getenv files
Type: `BUG` · Evidence: `CONFIRMED` · Priority: P1
Source(s): Original Zeus adoption audit (live-reproduced there); re-verified against current source this pass (`importer.py::_discover_python_variables`, unchanged).
Component: `core/importer.py`

- **Problem:** The moment `discover_python_env_vars` finds *any* recognized read anywhere in a `.py` file, the function returns *only* the discovered variables — every top-level literal assignment not independently recognized as a "read" is silently dropped.
- **Evidence:** Live-reproduced on Zeus's real `env_config.local.py` (59 of 61 variables dropped); code re-read this pass, confirmed unchanged.
- **Decision:** Open, backlog.

### BL-021 (aka G-2) — Static AST parsing can't see values set inside a conditional block
Type: `ARCHITECTURE` (documented limitation, intentional) · Evidence: `CONFIRMED` · Priority: P3
Source(s): Zeus audit; re-verified this pass.
Component: `parsers/_python.py`

- **Problem:** Only top-level `ast.Assign` nodes are read; `if os.environ.get("X") == "yes": DB_HOST = "db"` is invisible.
- **This is a deliberate tradeoff**, not an oversight: evaluating the conditional would mean executing untrusted repository code, which the parser's static-only design exists specifically to avoid (see BL-011's sibling security principle).
- **Decision:** Record as documented limitation. A narrow future direction (evaluate a single, simple `if os.environ.get(X) == Y:` block against real values at check-time — the same shape `requiredIf` already handles) is `EXPLORING`, not committed.

### BL-023 (aka G-4) — `service add` silently drops previously-set fields on a second call
Type: `BUG` · Evidence: `CONFIRMED` · Priority: P1
Source(s): Zeus audit (live-reproduced there); re-verified this pass by reading `config/manager.py::add_service` directly (lines 332–389) — confirmed unchanged: `entry: Dict[str, Any] = {"schema": schema_path}` builds a fresh dict from only the current call's arguments every time, replacing the prior entry wholesale.
Component: `config/manager.py`

- **Expected behavior:** Merge into the existing entry, matching `add_manifest`'s already-additive behavior.
- **Decision:** Open, backlog. Explicitly flagged (in the G-3 spec's own file map) as **not to be bundled into G-3's implementation** — independent fix.

### BL-024 (aka G-5) — No batch "reverse undeclared" / stale-schema-variable command
Type: `FEATURE` · Evidence: `CONFIRMED` (absence) · Priority: P3
Source(s): Zeus audit; re-verified this pass (no matching `@app.command()` exists in `cli.py`).
- **Decision:** Backlog / future.

### BL-025 (aka G-6) — No model for a Compose override/delta file
Type: `ARCHITECTURE` · Evidence: `CONFIRMED` · Priority: P3
Source(s): Zeus audit; re-verified this pass (no override-merge support found anywhere in `parsers/`/`config/manager.py`).
- **Note:** Explicitly distinct from G-3/BL-030 — this is a registration-time limitation (what *can* be registered), not a completeness-semantics one (what happens once something *is* registered).
- **Decision:** Backlog / future — a materially bigger feature (real Compose multi-file merge semantics) than G-3.

### BL-026 (aka G-7) — `check --help` undersells what a bare invocation does
Type: `DOCUMENTATION` / `CLI` · Evidence: `CONFIRMED` · Priority: P3
Source(s): Zeus audit; re-verified this pass — current help text still reads "Defaults to the project's (or service's) local file," no mention of deployment-manifest validation.
- **Decision:** Trivial, separate immediate fix candidate.

---

## Part 3 — Needs evidence (plausible, not independently reproduced this pass)

Sourced from the 2026-08-21 report's own text (a primary document, read in full) but not re-run live this pass. **Do not promote any of these to `CONFIRMED` without reproduction.**

- **BL-006 (RA-P1-5)** — Generator field-name collision (`API-KEY`/`API_KEY` both normalize to `api_key`). Structurally confirmed (the normalization function does produce the collision); the claim that the later assignment silently overwrites the earlier one was not traced end-to-end. `GENERATION`, P2.
- **BL-007 (RA-P1-8)** — Flaky `TestFindNearestGitBoundary` test (assumes no `.git` ancestor above the temp dir). `TECH_DEBT`, P3.
- **BL-009 (RA-P2-10)** — Dotenv multiline/unclosed-quote values silently mangled instead of rejected. `DISCOVERY`, P2.
- **BL-012 (RA-P2-13)** — Excluded-file diff-scan error-to-skip path (`_get_diff_lines()` failure → skip instead of "uncertain"). `SECURITY`, P2.
- **BL-013 (RA-P2-14)** — JS/TS discovery is pattern-based, `.js/.jsx/.ts/.tsx` only, misses `.mjs/.cjs/.mts/.cts` and dynamic access. `DISCOVERY`/`DOCUMENTATION` (positioning accuracy), P3.
- **BL-014 (RA-P2-15)** — Importer classification is advisory-only; an unrecognized secret can land in `defaultValue`. General version of BL-020's specific failure. `DOCUMENTATION`, P3.
- **BL-016 (RA-P3-17)** — Package metadata deprecation warnings (table-form license, MIT classifier) plus stale "configuration governance"/security-topic wording in `pyproject.toml`. `TECH_DEBT`, P3.
- **BL-017 (RA-P3-18)** — Dead legacy surface (`state.py`, unused profile exceptions) inviting accidental revival of the old profile/vault direction. `TECH_DEBT`, P3.
- **BL-018 (RA-P3-19)** — Generated TypeScript relies on `#private` fields without documenting a required target. `DOCUMENTATION`, P3.
- **BL-015 (RA-P3-16, aka EB-7)** — `pytest-cov`/coverage not set up. Corroborated by both the report *and* CLAUDE.md's own record independently — closer to `CONFIRMED` than the others in this section, kept here only because "absent" wasn't re-run as a literal command this pass. `TECH_DEBT`, P3.

---

## Part 4 — Stale / Refuted

Kept, not deleted, so nobody re-raises them without reason.

### BL-019 (aka EB-2) — REFUTED: "registered-but-missing manifest/local_file crashes `check`/`doctor`"
Evidence: `REFUTED`
- **Original claim:** `cli.py`'s outer `except EnvShieldException` handler wouldn't catch the bare `FileNotFoundError` every parser raises for a missing file, so a typo'd/uncommitted registered path would crash `check`/`doctor` uncaught.
- **What live testing found:** No crash, in any of three shapes tried (missing `.yml` manifest — `get_parser`'s content-sniffing returns `None` gracefully for a nonexistent file; missing `.py` local_file — `check_result` has its own **explicit, dedicated `except FileNotFoundError:` clause**, missed on the earlier source-only read; missing manifest in an otherwise-real directory).
- **Root cause of the false alarm:** `check_result` (`schema_manager.py`) already catches `FileNotFoundError` separately from `EnvShieldException`, before `cli.py`'s outer handler is ever reached — a pattern I hadn't read closely enough on the prior pass.
- **Decision:** Not an EnvShield finding. Dropped from active tracking. Kept here as a record that this was checked and found false — a methodology note, not a live issue.

---

## Part 5 — G-3 / `completeness: union`

### BL-030 (aka G-3) — Implement `completeness: union` per the Rev 3 specification
Type: `ARCHITECTURE` / `FEATURE` · Evidence: `CONFIRMED` problem, spec `STRATEGIC` (decided) · Priority: P1 (highest product value identified across this whole engagement)
Source(s): Original Zeus audit (live-reproduced there — `schema sync` tried to append 18 Compose-owned variables into a Python file); two full adversarial spec passes since.
Component: `config/manager.py`, `core/schema_manager.py`, `cli.py`, `core/doctor.py` (see the spec's own file map for exact functions)

- **Problem:** EnvShield's single-source model can't express a legitimate contract split across multiple registered sources (Zeus: mode-switches in Compose, secrets in a Python module) — a variable satisfied by one registered source is falsely reported missing because another source doesn't have it.
- **Chosen architecture:** `completeness: union` — a per-service, opt-in flag in `envshield.yml`; a set-union over *presence* only, never over values. Deliberately **not** the earlier "Satisfaction Modes" proposal (see BL-032) — no automatic source discovery, no precedence, no merge strategies, no per-field `source` schema annotation.
- **Status:** Fully specified across two adversarial review passes (Rev 2, Rev 3), published as a standalone spec artifact. Not yet implemented.
- **Implementation checklist** (each already resolved *within* the spec — tracked here as one item's sub-work, not as separate backlog rows, per the recommendation to avoid clutter):
  - `requiredIf` evaluated per-source independently, OR-reduced across sources (fixes a hidden-precedence bug found in Rev 2, backed by a worked counter-example).
  - `SourceResult`/`FieldStatus`/`FieldEvaluation` — a transient, discard-after-classification design; never retains raw values.
  - `check --json`'s new `combined` key — a top-level sibling to `results`, not a heterogeneous entry inside it, verified against two named existing tests (`test_check_json_reports_clean_state`'s exact-equality assertion, `test_json_shape_matches_check_doctor_convention`'s homogeneity rule).
  - `check`'s exit-code control flow restructured for union targets (evaluate all sources first, then decide, not an inline per-source flip).
  - "Registered-source completeness" and "Source health" kept as two structurally separate signals everywhere (CLI, JSON, `doctor`), so a source error can never hide behind a passing completeness line.
  - `doctor` wording corrected so a union-satisfied-but-locally-incomplete file is never described as self-sufficient.
  - `_evaluate_source`'s exception handling broadened to `(EnvShieldException, FileNotFoundError, OSError, SyntaxError)` plus an independent `ast.parse` pre-check for `.py` local files — see BL-031, the one item from this list that remains genuinely open.
  - The resolved-environment boundary made explicit everywhere in UI text: `completeness: union` validates registered, static sources — it is never a claim about the environment a running process actually receives.
  - `FieldStatus.PRESENT` (aka `G3-11`) — a guardrail note, not an implementation step: this status deliberately collapses "asserted" and "forwarded" values for completeness purposes; whoever builds provenance (`BL-033`) later must derive that distinction independently and must not reuse `FieldStatus.PRESENT` as if it already carried it.
  - The word "union" (aka `G3-12`) — a documentation note: `envshield.yml`'s `completeness: union` is a set-union over *presence*, not a Compose/Kustomize-style value merge; the spec's user-facing docs must say so explicitly so the two aren't conflated.
- **Dependencies:** BL-031 must be resolved before `_evaluate_source` is implemented.
- **Related:** BL-023 (explicitly must **not** be bundled into this implementation, despite being found in the same investigation).
- **Decision:** Architecture decided, spec ready. **Do not reopen Satisfaction Modes or runtime merge/precedence unless new evidence demonstrates the current architecture cannot solve a real user problem** (per explicit standing instruction).
- **Target:** Next implementation slot, pending BL-031's resolution and pending Part 0's release blockers being addressed first (a v1-quality release should not ship with confirmed secret-leakage/false-clean issues open, independent of G-3's own timeline).

### BL-031 (aka G3-10) — Parser exception content-safety verification
Type: `SECURITY` · Evidence: `NEEDS_EVIDENCE` · Priority: P1 (blocks BL-030's `_evaluate_source` specifically, not the architecture)
- **Problem:** Could a parser's exception message (`SyntaxError`, `YAMLError`-wrapped `EnvShieldException`, etc.) ever echo raw file content — a source line, an offending token — into `SourceResult.error` / the new `combined[].errors[]` JSON field?
- **Status:** Not yet checked line-by-line against every `raise` site in all four parsers. A five-minute read, not a design question — recommended to resolve *before* writing `_evaluate_source`, not after.
- **Partial evidence (2026-08-26, found during BL-001–BL-004 post-fix verification, not a dedicated BL-031 pass):** the Python parser's contribution to this question is now directly answered — `BL-002`'s fix confirmed and regression-tested (`test_python_parser_syntax_error_message_never_echoes_the_offending_line`) that `_python.py`'s `raise EnvShieldException(f"Could not parse '{file_path}': {e}")` never echoes file content, since `str(SyntaxError)`/`str(UnicodeDecodeError)` describe only the failure (position/description), never the source line itself — live-confirmed with a sentinel secret-shaped string placed on the offending line. This resolves one of the "four parsers" this item still needs checked; `_docker_compose.py`, `_kubernetes.py`, and `_dotenv.py`'s own `raise` sites remain unchecked. Not sufficient to close this item.
- **Decision:** Open, blocking BL-030's specific implementation of `_evaluate_source`. Python parser's portion answered (see Partial evidence); three parsers still need the same check.

### BL-032 (aka AD-1) — Satisfaction Modes rejected
Type: `ARCHITECTURE` (decision record) · Evidence: `STRATEGIC`
- **Decision:** Rejected in favor of `completeness: union` after a full comparative architecture review. Reasons on record: conflates contract semantics with source topology; automatic discovery introduces determinism problems; shell-environment discovery introduces security/reproducibility concerns; precedence answers a runtime-resolution question EnvShield should not own; merge strategies push EnvShield toward becoming a runtime configuration resolver; materially more complex than the actual problem requires.
- **Purpose of this entry:** Institutional memory, so it isn't re-proposed without whoever proposes it seeing why it was rejected.

### BL-033 (aka FUT-1) — Provenance ("which source satisfies this variable")
Type: `FEATURE` · Evidence: `STRATEGIC` · Priority: `EXPLORING`
- Designed only (`explain` provenance mockup, secret masking reusing the existing `<redacted, N chars>` convention). Not committed to a date. Depends on BL-030 shipping first.

### BL-034 (aka FUT-2) — Consistency / conflict detection ("do the sources that have it agree")
Type: `FEATURE` · Evidence: `STRATEGIC` · Priority: `EXPLORING`
- Designed only. Explicitly requires the "asserts vs. forwards" data-model distinction (case J — a bare `${VAR}` Compose forward) that doesn't exist yet; building this before that distinction exists would produce false positives on any legitimate forward. Depends on BL-030.

### BL-035 (aka FUT-3) — Runtime-boundary validation
Type: `ARCHITECTURE` · Evidence: `STRATEGIC` · Priority: `EXPLORING`, explicitly not scoped
- The question "what does the application process actually receive" (shell exports, launcher defaults, container-runtime injection) — real, named, explicitly **not** designed or committed to. Would need its own charter-level scoping discussion (touches §2/§4's existing boundaries) before any design work. Recorded so it isn't quietly built under the banner of "finishing" G-3.

---

## Part 6 — AI-Agent-Friendly EnvShield (strategic direction)

**Umbrella direction, not a single feature.** Do not implement MCP or any agent
interface yet — this section exists to hold the architectural questions until
they're answered, per explicit instruction.

### BL-040 — AI-Agent-Friendly EnvShield
Type: `AI_AGENT` · Evidence: `STRATEGIC` · Priority: `EXPLORING`
Source(s): AI/Varlock/MCP strategic discussion (this engagement).

- **Framing:** position as "AI-agent friendly," not "MCP scanner" — MCP scanning (BL-041) is one concrete feature within this direction, not the direction itself.
- **Open architectural questions, recorded verbatim, none answered yet:**
  1. What information can an agent safely request?
  2. What information must never be returned?
  3. How do we guarantee secrets are redacted?
  4. Should the agent interact with EnvShield through MCP specifically?
  5. Should EnvShield expose structured JSON APIs first and MCP second?
  6. What configuration graph/index (BL-042) would make agent queries useful?
  7. What questions should an agent actually be able to answer? Candidates on record: "What variables does service X depend on?", "Where is DATABASE_URL used?", "Which services are affected if DATABASE_URL becomes required?", "Is DATABASE_URL present in every deployment target?", "Which configuration variables are undeclared?", "Why is this deployment failing validation?", "Which configuration changes are security-sensitive?", "Does this MCP configuration expose a secret?"
- **Design principles already agreed, before any implementation:** deterministic structured responses; secret-safe output by construction; provenance/evidence attached to every answer; read-only inspection; explicit trust boundaries; no secret retrieval; no runtime secret resolution.
- **Cross-reference:** ROADMAP.md's existing "Next: AI-Agent Configuration Safety" and "Exploring: a more direct interface for AI agents (e.g. MCP)" are the same direction — this backlog item is the detailed record behind those two lines, not a competing plan.
- **Decision:** Not implemented. Architectural questions above must be answered before any code is written.

### BL-041 — MCP configuration scanning
Type: `MCP` · Evidence: `STRATEGIC` · Priority: `EXPLORING`
Source(s): Same discussion as BL-040.

- **Concept:** `envshield scan --mcp` or `envshield mcp scan` — inspect MCP configuration surfaces for unsafe secret exposure and configuration problems.
- **Explicitly a sub-feature of BL-040**, not a disconnected second product — must fit EnvShield's existing configuration-safety model (contract → discovery → deployment validation → secret safety), not become a separately-branded scanner.
- **Decision:** Not implemented. Blocked on BL-040's architectural questions, particularly #1–#4.

### BL-042 — Configuration Graph
Type: `ARCHITECTURE` · Evidence: `STRATEGIC` · Priority: `EXPLORING`
Source(s): AI/Varlock/MCP discussion; already named in ROADMAP.md's "Next" list (item 1, "Configuration Graph & Impact Analysis") — **same direction, not a new one.**

- **Concept:** `env.schema.toml` → schema + code + deployment → one queryable index → human CLI/CI and AI agents (BL-040) both consume it, never the raw filesystem directly.
- **Depends on:** Configuration Discovery (Phase 2B, already shipped) being the actual data source — not the regex-era scanner.
- **Decision:** Not implemented. Kept distinct from BL-033 (provenance) and BL-034 (consistency) — related, not automatically the same feature, per explicit instruction not to collapse these prematurely.

---

## Part 7 — Performance / Scalability (research, not yet benchmarked)

**No external report's numbers are trusted here.** Every item below exists to
*establish* real numbers, not to validate a claimed one.

### BL-050 — Large-repository / `undeclared` performance benchmark
Type: `RESEARCH` / `PERFORMANCE` · Evidence: `NEEDS_EVIDENCE`
- No real numbers exist yet for `undeclared`'s source-discovery pass on a large repository. First step is establishing a baseline, not architecture.

### BL-051 — Memory consumption under large schemas/discovery
Type: `RESEARCH` / `PERFORMANCE` · Evidence: `NEEDS_EVIDENCE`

### BL-052 — Incremental discovery / caching effectiveness
Type: `RESEARCH` / `PERFORMANCE` · Evidence: `NEEDS_EVIDENCE`
- **Checked before writing this item, per explicit instruction:** searched the codebase for any existing cache (`grep -rl "cache\|Cache\|lru_cache" envshield/`). **No EnvShield-authored caching mechanism exists anywhere** — every hit was either a git `--cached` flag or a directory-exclusion list (`__pycache__`, `.mypy_cache`) unrelated to EnvShield's own computation. Any future work here is greenfield design, not extension of an existing cache.

### BL-053 — Parallel/concurrent multi-service validation
Type: `RESEARCH` / `PERFORMANCE` · Evidence: `NEEDS_EVIDENCE`

---

## Part 8 — Pending reconciliation

### BL-060 — Comprehensive Technical Audit (2026-08-24) reconciliation
Type: `PRODUCT` / `TECH_DEBT` · Evidence: `NEEDS_EVIDENCE` (source document not available)
Source(s): Described by the user this session; the report itself is **not present in this repository** (checked `docs/reports/` and searched the whole tree — only the 2026-08-21 report exists as a file) and was not pasted in full.

**This is a source-availability tracking record, not a product or security finding.** It asserts nothing about EnvShield's current behavior. It exists so that "we were told a 2026-08-24 audit exists" isn't lost, and so nothing from it gets promoted to `CONFIRMED` by assumption once the document does surface.

- **What's known:** The report makes claims across architecture, CLI behavior, security, performance, scalability, discovery, deployment compatibility, codegen, and more. It is already known to contain **stale or incorrect claims** — e.g. it reportedly says `service add` doesn't exist (false; confirmed live and in source this engagement, see BL-023's own reproduction of a *different* bug in that exact command), and it uses CVE-style identifiers that must **not** be treated as real CVEs.
- **`check --all` claim, reconciled this pass:** the report is described as claiming `check --all` is missing. Checked directly against current `cli.py` — **confirmed true**: no `--all` flag exists anywhere in the `check` command today. This specific claim holds.
- **Decision:** Nothing from this report is promoted to `CONFIRMED` without the same live-verification methodology used for the 2026-08-21 report (see Part 0–2 above, and the standalone verification report this backlog is built from). Needs the actual document (file, link, or pasted text) before any further reconciliation is possible.
- **Target:** Blocked on obtaining the source document.

---

### BL-100 — 2026-08-10 security audit: outcome known, individual findings not representable
Type: `TECH_DEBT` (tracking) · Evidence: `NEEDS_EVIDENCE` (source document not available) · Priority: —
Source(s): Referenced only in CLAUDE.md/AGENTS.md §19 ("Phase 0 — all six P0 release blockers, P0-1 through P0-6, plus P1-1 — closed 2026-08-11").

**This is a source-availability tracking record, not an open security finding.** The six P0s and one P1 it refers to are already closed (per CLAUDE.md's own record, closed 2026-08-11) — this entry does not assert or imply that a vulnerability is currently present. Its `TECH_DEBT` type (not `SECURITY`) and blank priority are deliberate: it tracks a documentation gap (the individual findings can no longer be itemized), not a live risk.

- **What's known:** Six P0s and one P1 were found and closed. CLAUDE.md's §4 security invariants section names the shape of at least some of them (unredacted scan findings, unmasked validation-error text, a symlink-followed arbitrary-file-read misreported as a legitimate finding — the fix for the last one is independently corroborated: `config/manager.py::_ensure_within_project`'s `realpath`-based containment check exists in current code and its docstring explicitly names "the P0-6 vulnerability this guards against").
- **What's not available:** No report file exists anywhere in this repository (checked). The individual `P0-1` through `P0-6` and `P1-1` descriptions are not recoverable from anything available this session.
- **Decision:** Per the Finding and Evidence Management workflow's rule 8, this is recorded as a missing source, not reconstructed. **Do not invent `BL-NNN` entries for `P0-1`–`P0-6`/`P1-1`'s individual content.** If the original report or commit history (`d7e3a8c`, the symlink hardening pass named in §19) is ever located, split this into proper individual entries then.
- **Target:** Blocked on locating the source (report file or a sufficiently detailed commit trail).

### BL-101 — 2026-08-21 report items 1–3: PyPI/hosted-docs/website staleness claims, not independently verified
Type: `PRODUCT` / `DOCUMENTATION` · Evidence: `NEEDS_EVIDENCE` · Priority: P2
Source(s): 2026-08-21 release-readiness audit's own P0 narrative (its items 1–3, distinct from item 4 which is `BL-001`); CLAUDE.md §19's claim that a documentation/positioning pass addressed this.

**Unlike `BL-060`/`BL-100`, this is a real, substantive finding, not a source-availability record.** The source document is available and was read in full — the claims themselves are known; only independent live verification of whether they still hold is missing. `NEEDS_EVIDENCE` here means the ordinary thing it means elsewhere in this backlog (e.g. `BL-006`–`BL-018`): a real, specific, checkable claim awaiting reproduction — not "we don't have the document."

- **What the report claimed (2026-08-21):** PyPI was distributing `3.0.0`, not `4.6.0`, with stale secret-scanner-first copy; hosted docs were materially obsolete (missing `undeclared`, `explain`, service management, schema diff); the public website mixed current features with stale/absolute claims (e.g. "prevents secrets from ever reaching Git").
- **What CLAUDE.md claims happened since:** a documentation/positioning pass addressed README wording, the CLI tagline, and CHANGELOG wording — but this is about *repository* docs, not the *external* PyPI listing or hosted docs/website specifically.
- **Why this needs its own entry:** this same document trail already contained one confirmed-false "this was handled" claim (`BL-001`–`BL-004`'s "no P0/P1 findings"). That pattern is reason enough not to silently assume items 1–3 were also actually resolved just because a nearby claim says so.
- **What was checked this pass:** a local website repository exists at `/home/rabbil/dev/envshield_website`, last modified 2026-08-25 — after the audit, consistent with someone having worked on it — but its actual current content, the current hosted-docs content, and the current live PyPI listing were **not inspected this pass** (out of scope for a documentation-reconciliation task; this is a live-verification task like the one that produced `BL-001`–`BL-011`).
- **Decision:** Open, `NEEDS_EVIDENCE`. Recommend a live-verification pass (checking the actual PyPI page, hosted docs, and website repo content) the same way `BL-001`–`BL-011` were verified, before this is marked `CONFIRMED` fixed or reopened as a live finding.
- **Target:** Backlog — next live-verification pass, not this one.

---

## Part 9 — User feedback

### BL-070 — `.env.example`'s semantic role (DEV.to comment)
Type: `PRODUCT` · Evidence: `USER_FEEDBACK`
Source(s): External comment on an EnvShield `.env.example` blog post.

- **The conceptual point, preserved distinctly from BL-001:** an example file is normally documentation/onboarding material/a template, meant to be committed — it is *not* supposed to contain real secret values. This is a semantic-role observation about what `.env.example` is *for*, independent of any specific bug.
- **Relationship to BL-001:** this comment is the product evidence that *motivated* asking "what should EnvShield do with a secret field that has a default, or when generating `.env.example`" — a question this engagement later answered concretely by finding BL-001 (a real, live-reproduced release blocker). BL-070 is kept as its own entry specifically so the original user-facing framing isn't lost inside a bug report's implementation language.
- **Decision:** Context/product evidence. No separate action beyond BL-001 — track here so the "why we started looking" reasoning survives independent of the bug's own lifecycle.

---

## Part 10 — Research

### BL-080 — Varlock competitive positioning
Type: `RESEARCH` · Evidence: `RESEARCH`
Source(s): AI/Varlock/MCP strategic discussion; CLAUDE.md §2's own competitive table.

- **Finding:** EnvShield should not try to out-feature Varlock as a runtime environment/secrets loader. Strongest differentiation: configuration-as-code/contract validated across source+deployment boundaries; code-aware configuration analysis (AST discovery → eventual Configuration Graph, BL-042); deployment/CI safety without becoming a runtime resolver.
- **Cross-reference:** This is the same conclusion CLAUDE.md §2 already documents in more detail, with the explicit caveat that it's "only as good as its last evidence check; revisit when the competitive landscape moves." This entry exists so that re-verification is itself tracked as a discrete, dateable piece of work rather than assumed permanent.
- **Decision:** No action — recorded as the evidentiary basis behind an existing charter position.

---

## Part 11 — Explicitly not building yet

Cross-referencing rather than duplicating ROADMAP.md's own "What We're Deliberately Not Building" section (secret vault, out-detecting dedicated scanners, generic `.env` editor/chatbot, replacing typed config libraries) — that list stands unchanged by this pass.

Specific to this backlog's own findings:
- **Satisfaction Modes, automatic source discovery, source precedence, merge strategies** (BL-032) — rejected, not paused.
- **Runtime-boundary validation** (BL-035) — real, named, not scoped; needs its own charter-level discussion first.
- **MCP implementation, any AI-agent interface** (BL-040, BL-041) — architectural questions unanswered; no code until they are.
- **A generic project-level `completeness` default in `envshield.yml`** — considered and explicitly deferred inside the G-3 spec itself (not worth a new config surface for two lines saved on Zeus).

---

## Part 12a — Source Reconciliation Status

One row per source this backlog draws on. "Fully reconciled" means every
finding that source is known to contain has a `BL-NNN` mapping *or* an
explicit unresolved-limitation note — not that the source's claims were all
confirmed true.

**Two rows here point at source-availability tracking records, not product
findings:** the 2026-08-10 audit row (→ `BL-100`) and the 2026-08-24 audit
row (→ `BL-060`) exist because a *source document* is missing — they assert
nothing about EnvShield's current behavior. Every other "No"/"Partially" row
— in particular the 2026-08-21 audit row's `BL-101` — is the ordinary kind
of open item: the source is available, the claim is known, only
independent verification is outstanding.

| Source | Available? | Fully reconciled? | `BL-NNN` mapping(s) | Unresolved limitation |
|---|---|---|---|---|
| 2026-08-10 security audit | **No** — no report file exists in this repository; only its outcome is summarized in CLAUDE.md/AGENTS.md §19 | Outcome tracked; individual findings **not** reconstructable | `BL-100` (tracking only) | Individual `P0-1`–`P0-6`/`P1-1` content unrecoverable without the original report or a fuller commit trail. Not fabricated. |
| Zeus adoption audit | Yes — external artifact, its exact `G-1`–`G-7` text was re-fetched and quoted this engagement | **Yes** — all seven re-verified against current source | `BL-020`–`BL-026`; `G-3` → `BL-030` | None outstanding. |
| G-3 Rev 2 (adversarial pass) | Yes — this engagement's own published spec artifact | **Yes** | Folded into `BL-030`'s checklist | None outstanding. |
| G-3 Rev 3 (runtime-boundary re-review) | Yes — same artifact, revised | **Yes** — including the two items (`G3-11`, `G3-12`) missed on the first backlog-creation pass, added this pass | `BL-031`; `BL-030`'s checklist | None outstanding. |
| 2026-08-21 release-readiness audit | Yes — `docs/reports/2026-08-21-release-readiness-audit.md` | **Partially.** Every P0–P3 code-correctness finding (items 4–19) is reconciled with live-verification evidence. Items 1–3 (PyPI/hosted-docs/website staleness) are tracked but not independently re-verified. | `BL-001`–`BL-018`; items 1–3 → `BL-101` | `BL-101` needs its own live-verification pass (PyPI listing, hosted docs, website repo content) before it can be marked resolved either way. |
| 2026-08-24 comprehensive audit | **No** — not present in this repository, not pasted in full | No — cannot reconcile without the document | `BL-060` (tracking only) | Entirely blocked on obtaining the source document. Known-stale example claims (`service add` doesn't exist) already refuted without needing the full document. |
| AI/Varlock/MCP strategic discussion | Yes — this engagement's own conversation record | **Yes** | `BL-040`, `BL-041`, `BL-042`, `BL-080` | None outstanding — these are `STRATEGIC`/`RESEARCH` entries by nature, not findings awaiting verification. |
| DEV.to `.env.example` feedback | Yes — this engagement's own conversation record | **Yes** | `BL-070` | None outstanding. |
| "3.8" / "3.10" numbering | **No** — referenced only by CLAUDE.md/AGENTS.md §19, source of the numbering scheme itself unknown | No — cannot map to anything | None (explicitly unmapped) | Genuinely unresolved. Not converted into a fabricated `BL-NNN` entry — the numbering is preserved as a named, open loose end in §19 and here, nothing more. |

---

## Part 12 — Source traceability index

| Source | Findings it produced |
|---|---|
| Original Zeus adoption audit | BL-020–BL-026 (G-1–G-7), motivated BL-030 |
| G-3 architecture review (Satisfaction Modes) | BL-032 |
| G-3 implementation spec, Rev 2 adversarial pass | BL-030's checklist (requiredIf, JSON shape, exit-code flow, source-health separation, doctor wording) |
| G-3 implementation spec, Rev 3 (runtime-boundary re-review) | BL-031, the resolved-environment-boundary item in BL-030's checklist, `G3-11`/`G3-12` (also folded into BL-030's checklist) |
| 2026-08-21 release-readiness audit (document) | BL-001–BL-018 (as `RA-*`), sourced; items 1–3 of the same document → BL-101 |
| This engagement's live verification pass | BL-001–BL-005, BL-008, BL-010, BL-011 upgraded to `CONFIRMED`; BL-019 (`EB-2`) refuted |
| This session's own repo inspection | BL-090 (`AGENTS.md` drift), BL-052's caching finding, BL-101's website-repo existence check, BL-091 (`v4.6.0` release-status note's tag-date/commit-count mismatch, found while checking git tags for the "Versioning and Release Cadence" policy), BL-092/BL-093 (parser error-handling gaps found while investigating `BL-002`'s root cause), BL-094 (TypeScript numeric-coercion gap found while investigating `BL-003`'s root cause), BL-095 (silent scan-skip gaps found while investigating `BL-004`'s root cause) |
| AI/Varlock/MCP strategic discussion | BL-040, BL-041, BL-042, BL-080 |
| DEV.to external comment | BL-070 |
| Comprehensive Technical Audit (2026-08-24) | BL-060 only — content not yet available for further reconciliation |
| 2026-08-10 security audit | BL-100 (tracking only — see Part 12a) |
| CLAUDE.md's own record | BL-090 (dating comparison), BL-100 (audit-outcome summary); the "3.2–3.10" numbering referenced there remains unmapped to anything in this backlog — flagged, not guessed at |

---

*Last consolidated: 2026-08-26. Same-day follow-ups: (1) `CLAUDE.md`/`AGENTS.md` reconciled against this file's findings (`BL-090` fixed; `BL-001`'s `CLAUDE.md`-contradiction note resolved). (2) A source-reconciliation verification pass added `BL-100`/`BL-101` (previously-unmapped 2026-08-10-audit and 2026-08-21-report-items-1–3 gaps), folded `G3-11`/`G3-12` into `BL-030`'s checklist (previously missing entirely), and added Part 12a. (3) Both charter files gained an "Engineering Task Workflow" index section and a corrected §22 commit-boundary diagram. (4) `BL-001` implemented and fixed — the first Part 0 blocker actually closed in code, not just documented; two previously-undiscovered manifestations of the same root cause (`explain.py`, `schema_manager.py`'s `_source_label`) were found and fixed in the same pass, and two pre-existing tests that had locked in the old buggy behavior as "expected" were corrected. (5) A durable "Versioning and Release Cadence" section was added to both charter files (between §22 and §23), and `BL-091` was opened for a stale tag-date/commit-count claim found in §19's "Release status" note while checking git tags for that work. (6) `BL-002` was fully investigated per the Engineering Task Workflow (root cause, exhaustive caller trace, proposed fix, test plan) but **not implemented** — per explicit instruction, this was an investigation-only pass. Two related-but-distinct parser error-handling gaps found during that investigation were recorded as `BL-092` (confirmed, live-reproduced) and `BL-093` (needs evidence — a live-reproduction attempt didn't reach the suspected code path). (7) `BL-002` and `BL-092` were then implemented and fixed, per the investigation's own proposed fix, plus companion except-clause updates at four call sites to preserve their existing skip-and-continue behavior — the second and third Part 0/adjacent blockers actually closed in code. `BL-093` was deliberately left open and unimplemented, as instructed; a cross-reference was added to its own entry. (8) `BL-003` and `BL-004` were then independently investigated per the Engineering Task Workflow (both re-reproduced live against current source; root cause confirmed unchanged for each) but **not implemented** — investigation-only pass. `BL-003`'s investigation found a related, lower-severity broader-failure-class finding recorded as `BL-094` (TypeScript numeric-type coercion), and a testing-infrastructure gap (no `zod`/`node` package management exists in this repo to runtime-test a fix). `BL-004`'s investigation confirmed EnvShield's own generated pre-commit hook is a direct victim of the exact bug (strengthening its existing evidence, not changing its status), and found a required fix-scope dependency — staged-mode's size-skip check currently runs before exclusion filtering, which would become a new false-failure source the moment `BL-004` is fixed naively — recorded inside `BL-004`'s own entry rather than as a separate item, since it's a correctness precondition for that fix, not an independent bug. (9) `BL-003` was then implemented and fixed, per its own investigation's proposed fix — the fourth Part 0 blocker actually closed in code (`BL-004` is now the only one remaining open). `BL-094` was left open and untouched, exactly as instructed — confirmed the `int`/`float`/`port` branch of `generator.py::_zod_base_type` (the code `BL-094` names) was not modified by this change. `BL-004` was not modified except that this fix's completion is reflected in Part 0's own status line. (10) A dedicated design/investigation pass (not implementation) was then run for `BL-004` — root cause and the exclusion-ordering gap re-confirmed live, a corrected ordering fix determined (size checks must not apply to a bounded diff-only scan of an excluded file), design options A/B/C/D evaluated, and a recommended B+C semantic model specified (a new `complete: bool` field alongside an unchanged `clean`; exit code fatal on incompleteness for `--staged` and/or `--json`, informational-only for a bare interactive `scan`). Inspecting adjacent code for the same failure class surfaced two more live-reproduced silent-skip mechanisms (symlink protection, unreadable-file handling) plus one code-read-only case (diff-computation failure on an excluded staged file) — recorded as new finding `BL-095`, explicitly not implemented, with the important scoping finding that the two live-reproduced sub-cases don't reach `--staged` mode (git's own object-store-based staged-content retrieval sidesteps both), so they don't defeat the pre-commit hook the way `BL-004` itself does — hence a separate, lower (P1) priority. (11) `BL-004` was then implemented and fixed exactly per its own approved design — the fifth and final Part 0 blocker actually closed in code (all four release blockers now fixed and committed, none yet released). The staged-mode exclusion/size-check ordering was corrected, `scan_result` gained the additive `complete` field, and exit-code fatality was wired to `--staged`/`--json` while a bare interactive scan's exit code was left unchanged, matching the approved contract exactly. The installed pre-commit hook required zero changes, confirmed via a real end-to-end `hook install` + commit test. One internal contradiction in this same entry's own prior "Required regression tests" bullet (two mutually inconsistent statements about `test_scan_reports_skipped_large_files`'s exit code) was found during implementation and resolved in favor of the design's dominant, three-times-repeated B+C intent rather than silently picking one — documented in `BL-004`'s own `Implementation` note rather than silently changed. `BL-095` was confirmed untouched (none of its three named code sites appear in this fix's diff) and its `Target` line updated to reflect that the `complete` field it depends on now exists. (12) A dedicated post-release-blocker verification pass then independently re-tested all four fixes' underlying failure classes (not just the original reproductions) against the current committed code — `BL-001` re-verified including a previously-untested `extends`-inheritance case (a secret+default conflict originating in an inherited base schema is correctly caught); `BL-002` re-verified including a non-companion caller (`setup_manager.py`'s `_read_seed_values`, which has no direct `EnvShieldException` handling of its own but correctly propagates to a clean top-level CLI error rather than crashing) and reconfirmed `BL-005` remains an accurate, distinct, unaffected finding (`schema sync --check`'s unconditional-`True` short-circuit for `.py` files never even reaches the parser `BL-002` fixed); `BL-003` and `BL-004` re-verified end-to-end including the real installed pre-commit hook. No new P0/P1 finding was discovered. `BL-093`/`BL-094`/`BL-095` and the shared parser infrastructure (`_base.py`/`_deployment.py`/`factory.py`) were confirmed untouched by any of the four fixes via direct diff inspection, not assumption. `BL-031` gained a partial-evidence cross-reference: `BL-002`'s fix and regression test directly answer this item's question for the Python parser specifically (three parsers remain unchecked). Part 0's status line and `BL-003`/`BL-004`'s own `Decision`/`Target` lines were corrected from "pending commit" to "committed," which had gone stale the moment the commits mentioned in the prior instruction actually landed. `CLAUDE.md`/`AGENTS.md` §19 was found to still describe `BL-003`/`BL-004` as open and v4.6.0 as blocked on all four — a real, reported inconsistency, deliberately **not corrected in this pass** (verification-only; see the session's own report for the exact stale text and proposed correction, pending explicit authorization). (13) `CLAUDE.md`/`AGENTS.md` §19's stale claim from (12) was corrected — both now accurately state all four of `BL-001`–`BL-004` are fixed and committed, none released, `v4.6.0` remains the latest released/tagged version. A dedicated investigation then substantially deepened `BL-005` and `BL-095`, without implementing either. `BL-005` was live-proven, via a real installed pre-commit hook and a real `git commit`, to bypass that same hook for any `.py`-local_file service with an ordinary, un-special-cased schema edit — priority elevated `P1` -> `P0` on this evidence. `BL-095` was re-tested against the now-shipped `BL-004` fix and confirmed all three sub-cases still produce `"clean": true, "complete": true"` — the new field does not cover any of them; sub-case 3 (diff-computation failure) was upgraded from code-reading-only to directly tested via a realistic simulated failure, with the important caveat that its trigger conditions are narrower than sub-cases 1/2's. No new P0/P1 finding was discovered; a targeted adjacent-code sweep for `BL-005`'s specific short-circuit pattern found no recurrence elsewhere. `BL-031` (already partial-evidenced in item 12) was not touched further. See each item's `Decision` line for current status — this file is the thing to update, not a substitute for actually doing the work.*
